# Claude Code Routines

Use the prompt files in `routines/prompts/`. Configure environment variables in the remote routine environment, not by writing `.env`. Start with paper mode. Push is optional and risky; keep `ALLOW_GIT_PUSH=false` until reviewed.
