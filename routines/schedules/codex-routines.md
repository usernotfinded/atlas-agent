# Codex Routines

Use the same prompt files in `routines/prompts/`. Codex or any local agent should use `omni-trade` CLI, Markdown memory files, and broker/provider adapters rather than hidden behavior.

