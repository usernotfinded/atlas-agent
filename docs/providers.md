# Providers

Providers implement `AIProvider.generate()`. Supported adapters include OpenAI-compatible endpoints, Anthropic, OpenRouter, local commands, and a deterministic null provider for tests.

Provider output is untrusted and advisory.

