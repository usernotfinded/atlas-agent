# v0.6.16 CAND-002 Multimodel Review Packet

> **Status:** planning-only documentation. This packet contains review prompts; it does not record or imply that any external model review has occurred.
>
> **Scope:** `feat(cand-002): add autonomous paper scorecard gate` (commits after `ba167215626a9e8ee949900f7aa5562fb0671058`).
>
> **Not financial advice.** Atlas Agent is a software tool, not a financial advisor. Trading involves significant risk of loss.

## Candidate summary

CAND-002 introduces:

- `atlas agent autonomous-scorecard` — a deterministic, offline scorecard command for autonomous-paper artifacts.
- `src/atlas_agent/agent/autonomous_paper_scorecard.py` — scorecard builder, promotion gate, Markdown renderer, and report writer.
- `docs/autonomous-paper-scorecard.md` — design, safety-boundary, and usage documentation.
- `scripts/check_autonomous_paper_scorecard_contract.py` and `tests/test_autonomous_paper_scorecard_contract.py` — deterministic static contract checks.
- `tests/test_autonomous_paper_scorecard.py` — scorecard and CLI smoke tests.
- `scripts/demo_autonomous_paper_scorecard.sh` — offline demo script.
- Updates to `docs/bounded-live-autonomy-governance.md`, `docs/releases/v0.6.16-*`, `CHANGELOG.md`, and integration into `scripts/dev_check.sh` / `scripts/release_check.sh`.

No live trading, live submit, broker order submission, real provider execution, source/package version bump, tag, GitHub Release, or PyPI publication is included.

## Model-specific review prompts

### GPT-5.5 — Architecture / product risk

Review the architecture and product-risk posture of CAND-002. Focus only on files changed for CAND-002.

1. Does the scorecard remain cleanly separated from live broker adapters, live submit paths, and real provider execution? Confirm there is no accidental bridging code between `src/atlas_agent/agent/autonomous_paper_scorecard.py` and broker submit APIs.
2. Is the scope appropriate for a planning candidate? Verify the change is paper-only, offline, deterministic, and does not alter protected runtime boundaries under `src/atlas_agent/config`, `brokers`, `execution`, `safety`, or `risk`.
3. Does the promotion gate avoid product claims that could be read as live-trading readiness, profit guarantees, or risk elimination? Check `docs/autonomous-paper-scorecard.md`, `docs/bounded-live-autonomy-governance.md`, and `CHANGELOG.md` for cautious wording.
4. Are the scorecard dimensions and promotion states reviewable, conservative, and fail-closed? Confirm `blocked` is the default and `eligible_for_shadow_live_review` is explicitly not live readiness.
5. Identify any architectural overreach that should be split out of CAND-002 before it could be considered for the next planning gate.

### Claude Opus 4.6 Thinking — Safety / security / governance

Review CAND-002 from a safety, security, and governance perspective. Treat every boundary as fail-closed until proven otherwise.

1. Confirm the hard invariants in `docs/bounded-live-autonomy-governance.md` remain intact. Specifically: live trading disabled by default, live submit disabled by default, provider output never execution authority, `RiskManager` mandatory and deterministic, kill switch mandatory, approval queues mandatory where applicable, and audit hash-chain present.
2. Verify `build_autonomous_paper_scorecard` cannot be coerced into live mode. Examine how inputs are parsed; check for any path where `live_trading_enabled`, `broker.submit`, or `provider.execute` could be evaluated or acted upon.
3. Check that no secrets, credentials, API keys, or private broker state are loaded, logged, or committed by the new code or tests. Confirm the audit-redaction dimension flags secret-like strings.
4. Review the scorecard promotion logic for clarity that `eligible_for_shadow_live_review` is planning-only, does not enable shadow-live execution, and does not mutate broker state.
5. Assess whether the static checker (`scripts/check_autonomous_paper_scorecard_contract.py`) enforces the documented boundaries deterministically without network access, credentials, or subprocess execution.
6. Flag any governance gap that would allow a future shadow-live implementation to bypass the staged autonomy ladder or existing approval gates based on a scorecard result.

### Gemini 3.1 Pro — Docs / consistency

Review CAND-002 documentation and cross-references for consistency, accuracy, and conservative messaging.

1. Verify every document touched by CAND-002 states its status as planning-only and includes a "Not financial advice" disclaimer.
2. Confirm terminology is consistent across `docs/autonomous-paper-scorecard.md`, `docs/autonomous-paper-loop.md`, `docs/bounded-live-autonomy-governance.md`, `docs/releases/v0.6.16-*.md`, and `CHANGELOG.md`. Check for mixed use of "autonomous paper," "shadow live," "scorecard," and "live trading."
3. Check that all required safety-boundary phrases are present: "paper-only," "offline," "no live trading," "no broker order submission," "RiskManager," "deterministic," and explicit denial of autonomous live-trading readiness.
4. Verify cross-references: `autonomous-paper-scorecard.md` links to `autonomous-paper-loop.md`, `bounded-live-autonomy-governance.md`, and `shadow-live-readiness-contract.md`.
5. Confirm `CHANGELOG.md`, release candidate docs, and the candidate-selection doc agree that CAND-002 is implemented in planning only, with no version bump, tag, GitHub Release, or PyPI publication.
6. Identify any doc phrasing that could be misread as a readiness claim, profit claim, or risk elimination claim.

### Kimi k2.7-code — Implementation / regression

Review the CAND-002 implementation for correctness, test coverage, and regression risk.

1. Read `src/atlas_agent/agent/autonomous_paper_scorecard.py` end-to-end. Confirm it never calls a broker, never loads credentials, never reaches a live order-submission path, and uses only local artifact files.
2. Verify the scorecard evaluates all 11 required dimensions and produces one of the four promotion states with a `blockers` list.
3. Review `tests/test_autonomous_paper_scorecard.py` for coverage of valid scorecard generation, missing/malformed artifacts, risk-blocked runs, no-trade runs, kill-switch blocked runs, replay mismatch, unsafe live/provider/broker references, redaction, promotion defaults, and CLI smoke.
4. Run the relevant checkers and tests:
   - `python scripts/check_autonomous_paper_scorecard_contract.py`
   - `pytest tests/test_autonomous_paper_scorecard.py tests/test_autonomous_paper_scorecard_contract.py -v`
5. Check that `scripts/dev_check.sh` and `scripts/release_check.sh` correctly wire the new checker and tests without bypassing existing gates.
6. Look for regressions in existing CLI commands, config loading, or event schemas caused by the new `autonomous-scorecard` subcommand.

## Review output format

For each model prompt, produce a concise findings list with one of the following dispositions per item:

- **PASS** — no issue, boundary or claim is sound.
- **QUESTION** — needs clarification from the author or another reviewer.
- **BLOCKER** — must be resolved before CAND-002 can advance in planning.

Do not assign an overall release readiness verdict. The purpose of this packet is to surface focused review input for human triage.

## Verification baseline

At the time this packet was prepared, the following commands passed on the CAND-002 implementation branch:

```text
$ python scripts/check_autonomous_paper_scorecard_contract.py
Autonomous paper scorecard contract check PASSED

$ pytest tests/test_autonomous_paper_scorecard.py tests/test_autonomous_paper_scorecard_contract.py -q
..............................
```

---

*This packet is documentation-only and does not authorize any release, tag, package publication, live trading enablement, or external model invocation.*
