# Safety

Hard rules: no secrets in git, no profit claims, no live trading by default, no AI direct-to-broker path, mandatory risk manager, kill switch override, manual live approval by default, and auditable orders.

