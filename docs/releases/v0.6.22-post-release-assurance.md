# v0.6.22 Post-Release Assurance

> **Release:** `v0.6.22`
> **Source/package version:** `0.6.22`
> **Status:** current public GitHub release
> **PyPI:** not published
> **Not financial advice.** Atlas Agent is a software tool, not a financial
> advisor. Trading involves significant risk of loss.

This document is the deterministic public assurance record for the
owner-authorized `v0.6.22` GitHub-only release, captured after the cutover. It
records the verified release state, local checks re-run during post-release
stabilization, and the CI status placeholder. `v0.6.21` is the historical
previous public release and `v0.6.23` is the next planning line. This is a
GitHub-only release record; it is **not** a live-trading, production-readiness,
or unattended-safe-operation claim.

## Release State

| Field | Value |
|---|---|
| Current public release | `v0.6.22` |
| Source/package version | `0.6.22` |
| Previous public release | `v0.6.21` |
| Next planned release | `v0.6.23` |
| Release type | GitHub-only |
| Release commit | `25570fa824e3695ff9ecc14ed6b2c1c8fa690438` |
| Annotated tag | `v0.6.22` (dereferences to the release commit, local and on `origin`) |
| GitHub Release | `v0.6.22` (created, not draft, not prerelease, latest) |
| GitHub Release assets | 0 (no uploaded artifacts) |
| PyPI | unpublished |

## Tag and GitHub Release Verification

- The annotated tag `v0.6.22` exists locally and on `origin` and dereferences to
  the release commit `25570fa824e3695ff9ecc14ed6b2c1c8fa690438`
  (`git ls-remote --tags origin 'v0.6.22^{}'`).
- The GitHub Release `v0.6.22` exists, is not a draft, is not a prerelease, is
  marked latest, targets `main`, and has 0 attached assets:
  <https://github.com/usernotfinded/atlas-agent/releases/tag/v0.6.22>.
- No new tag or GitHub Release was created by this post-release pass beyond the
  publication step above.

## GitHub Actions / CI Status

CI is green on the release commit `25570fa`. The tag push and release creation
occurred after the local verification pass; workflow run IDs for the `v0.6.22`
tag can be found at <https://github.com/usernotfinded/atlas-agent/actions>. Update
this section with specific run IDs once the tag-triggered workflows complete.

## Local Checks Re-Run (Post-Release Stabilization)

All of the following were re-run locally and passed during this pass:

- `check_version_consistency`, `check_release_metadata`,
  `check_public_docs_consistency`, `check_candidate_chain`,
  `check_forbidden_claims`, `check_bounded_autonomy_governance`,
  `check_cli_command_compatibility`, `check_safety_atomic_write`,
  `check_bounded_live_autonomy_readiness_contract`.
- `tests/test_candidate_chain.py`, `tests/test_public_docs_consistency.py`,
  `tests/test_provider_artifact_golden_contracts.py`, the CAND-015
  bounded-live-autonomy-readiness suites
  (`tests/test_bounded_live_autonomy_readiness.py`,
  `tests/test_bounded_live_autonomy_readiness_cli.py`,
  `tests/test_bounded_live_autonomy_readiness_contract.py`,
  `tests/test_bounded_live_autonomy_readiness_import_trace.py`).
- `atlas run --mode live` exits `2` (fail-closed) as expected.

Expected non-blocking warnings from `check_candidate_chain.py` are documented in
[v0.6.22 Release Notes](v0.6.22.md) and are unchanged from prior releases.

## PyPI and Artifacts

- PyPI remains **not published**; `pypi_published` is `false` in release metadata.
- No `dist/`, `build/`, or tracked `*.egg-info/` artifacts are uploaded.
- The GitHub Release will carry 0 assets once created.

## Safety Boundaries (Unchanged)

This release and this post-release pass change no runtime trading, broker,
provider, credential, or safety behavior:

- Live trading and live submit remain disabled by default; `atlas run --mode live`
  remains fail-closed.
- Provider and broker execution defaults are unchanged and disabled; no real
  brokers or providers are called and no credentials are loaded.
- `RiskManager`, approval queues, kill switch, heartbeat, deadman, the audit
  hash-chain, live-submit opt-in, and `can_submit` are not bypassed or changed.
- The CAND-014 provider-artifact extraction boundary remains strictly at
  `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.

## Remaining Warnings and Caveats

1. **CAND-016** acceptance verdict is `PASS`; see the
   [v0.6.22 release notes](v0.6.22.md) for the candidate details.
3. **CAND-015** retains a clean-room independence caveat: implementation and
   implementation-review were performed by the same agent system, so a
   clean-room independent review remains outstanding. No external or human
   review evidence is claimed or invented.
4. **CAND-014 Phase 2** is a strict one-module pilot extraction; no extraction
   beyond `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`
   is authorized.
5. A full `pytest` run or full `bash scripts/release_check.sh` may exceed local
   interactive timeouts. The targeted checks and `--quick` gate pass.

## Statement

`v0.6.22` is a GitHub-only governance/documentation/safety-check release. This
assurance record confirms release identity and process integrity only. It makes
no profitability, trading-safety, live-readiness, production-readiness, or
unattended-safe-operation claim.
