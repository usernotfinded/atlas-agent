# v0.6.27 Candidate Selection Gate

> **Status:** planning (`v0.6.27` next planned release line; not released)
> **Current public release:** `v0.6.26`
> **Previous public release:** `v0.6.25`
> **Next planned release:** `v0.6.27`
> **PyPI:** not published
> **Tag created:** no
> **GitHub Release:** not created
> **Not financial advice.** Atlas Agent is a software tool, not a financial
> advisor. Trading involves significant risk of loss.

`v0.6.26` is the current public GitHub release. `v0.6.25` is the historical
previous public release. No candidates are currently proposed or accepted for
the `v0.6.27` candidate chain. `v0.6.27` remains planning-only and is not
released. Opening this planning line authorizes no live trading, live submit,
order placement, broker execution, provider execution, credential loading,
network access, or approval queue mutation.

Current public release: `v0.6.26`. Source/package version: `0.6.26`. PyPI is not
published. `v0.6.27` is planning-only; no `v0.6.27` tag, GitHub Release, or PyPI
publication exists.

## Proposed candidates

| id | title | status | accepted |
|---|---|---|---|
| CAND-032 | Deferred domain loading for CLI startup | proposed | no |
| CAND-033 | Wire the declared daily-loss and leverage limits | proposed | no |
| CAND-034 | Map the research error codes the CLI flattens | proposed | no |

CAND-032, CAND-033, and CAND-034 are proposed only. None is accepted, designed,
or implemented, and none authorizes anything at runtime — CAND-033 can only
tighten the deterministic risk gate, and CAND-034 only widens a vetted status
allowlist. All three are measured rather than estimated —
the numbers behind them are recorded in
[v0.6.27 Release Candidates](v0.6.27-candidates.md) and
[safety-invariant-audit-followups.md](../development/safety-invariant-audit-followups.md)
— and each is brought to this gate for a decision rather than with a fix
already chosen.

## Selection rules

Any future `v0.6.27` candidate must satisfy the repository's candidate selection
rules before acceptance:

1. No live trading enablement — fail-closed defaults unchanged.
2. No broker/provider execution enablement — no direct AI-to-broker path.
3. No secrets or credentials committed.
4. No unsafe marketing claims — no promises of specific returns, eliminated
   risk, live readiness, or autonomous trading readiness.
5. Protected runtime boundaries preserved — `RiskManager`, approval queues, kill
   switch, heartbeat, deadman, audit hash-chain, live-submit opt-in, and
   `can_submit` are not bypassed.
6. Small, reviewable candidate batches that are independently valuable.
7. Deterministic test and documentation coverage.
8. Green verification before acceptance.
9. The CAND-014 provider-artifact extraction boundary is not broadened beyond
   `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.27` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

This candidate selection gate is documentation/governance only. The `v0.6.27`
release cutover is a separate future step and has not started.
