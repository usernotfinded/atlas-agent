# Release v0.5.7.dev31 — Provider Response Intake Policy Artifact

## Summary

This release adds local provider response intake policy artifacts and their integration into the research chain. No real provider response handling is added. No network calls. No API keys. No provider SDKs. No broker changes. No credentials are loaded.

Batch 8.4 introduces a deterministic, auditable response intake policy artifact that defines how future provider responses must be received, treated as untrusted, redacted, hashed, reviewed, and prevented from directly affecting trading/broker/live execution. It is created from a provider outbound payload preview and records safe policy metadata only: response storage, redaction, validation, review, unsafe response, trading separation, hash, and manual review policies. No raw provider response body, no raw request body, no credentials, no absolute paths, and no trading instructions are stored. Provider execution remains disabled.

## What Changed

### New Artifact

- `src/atlas_agent/research/provider_response_intake_policy.py`
  - `build_provider_response_intake_policy_dict()`: deterministic response intake policy artifact construction from a payload preview.
  - `create_provider_response_intake_policy()`: safe creation with validation, lineage, hash, and denylist scanning.
  - `safe_validate_provider_response_intake_policy_data()`: strict validation with invalid sentinel returns.
  - `validate_provider_response_intake_policy_artifact()`: detailed per-check validation.
  - `replay_provider_response_intake_policy()`: deterministic hash replay from source payload preview.
  - `iter_provider_response_intake_policy_artifacts()`: safe listing with invalid sentinels for tampered items.
  - `summarize_provider_response_intake_policy_state()`: read-only summary without writing artifacts.
  - `provider_response_intake_policy_sha256()`: deterministic canonical hash excluding volatile fields.
  - Policy substructures:
    - `response_storage_policy`: `raw_provider_response_stored=False`, `response_body_stored=False`, `response_body_hash_required=True`, `bounded_summary_allowed=True`, `artifact_storage_allowed_after_redaction=True`.
    - `response_redaction_policy`: `redaction_required_before_output=True`, `secrets_redacted=True`, `absolute_paths_redacted=True`, `broker_content_redacted=True`, `raw_secret_echo_allowed=False`.
    - `response_validation_policy`: `response_schema_validation_required=True`, `response_hash_required=True`, `unsafe_content_detection_required=True`, `trading_action_detection_required=True`, `manual_review_required_on_invalid_response=True`.
    - `response_review_policy`: `manual_review_required=True`, `auto_accept_response_allowed=False`, `auto_execute_response_allowed=False`, `auto_create_order_allowed=False`.
    - `unsafe_response_policy`: `unsafe_response_behavior="manual_review_required"`, `malformed_response_behavior="fail_closed"`, `forbidden_fragment_behavior="fail_closed"`, `trading_instruction_behavior="manual_review_required"`, `broker_instruction_behavior="fail_closed"`.
    - `trading_separation_policy`: `provider_response_is_trade_signal=False`, `provider_response_can_create_pending_order=False`, `provider_response_can_approve_order=False`, `provider_response_can_submit_order=False`, `broker_live_bridge_allowed=False`.
    - `response_hash_policy`: `response_hash_required=True`, `hash_algorithm="sha256"`, `canonical_json_required=True`.
    - `manual_review_policy`: `human_review_required_before_any_trading_interpretation=True`, `human_review_required_before_any_future_broker_bridge=True`, `review_can_still_not_create_orders=True`.
  - 23 boolean safety flags including `provider_enabled`, `network_enabled`, `credentials_loaded`, `credential_value_present`, `credential_lookup_attempted`, `env_read_attempted`, `dotenv_loaded`, `provider_call_allowed`, `actual_provider_call_made`, `outbound_request_sent`, `provider_response_received`, `provider_response_trusted`, `provider_response_imported`, `provider_response_reviewed`, `provider_response_can_create_orders`, `provider_response_can_approve_orders`, `provider_response_can_call_broker`, `future_provider_execution_possible`, `trading_signal_generated`, `approval_created`, `pending_order_created`, `broker_touched`.
  - Impossible boolean detection: `provider_response_intake_policy_impossible_boolean` for tampered flags.
  - denylist-clean manifest: stores only safe metadata; never stores raw forbidden fragments.

### New CLI Commands

- `atlas research provider-response-intake-policy PROVIDER_OUTBOUND_PAYLOAD_PREVIEW_ID`
  - Create a response intake policy artifact from a payload preview.
- `atlas research provider-response-intake-policy-list`
  - List response intake policy artifacts with safe sentinels.
- `atlas research provider-response-intake-policy-show POLICY_ID`
  - Show one response intake policy artifact.
- `atlas research provider-response-intake-policy-validate POLICY_ID`
  - Validate a response intake policy artifact with multiple checks.
- `atlas research provider-response-intake-policy-replay POLICY_ID`
  - Replay and compare deterministic hashes.
- `atlas research provider-response-intake-policy-summary RUN_ID`
  - Read-only summary without writing artifacts.

All commands remain configless, local-only, and read-only where applicable.

### Integration

- `check-artifacts` now counts and validates provider response intake policy artifacts.
- `timeline` now links intake policy artifacts under their source payload preview.
- `dossier` now includes intake policy artifacts in the research chain summary.
- Demo workflow extended with intake policy creation, validation, replay, summary, and timeline lineage checks.

### Updated Documentation

- `CHANGELOG.md`: added dev31 entry for Batch 8.4.
- `README.md`: updated current status to v0.5.7.dev31.
- `docs/research-workflow.md`: added new CLI commands to the command overview.

### Version Bump

- `pyproject.toml`: 0.5.7.dev31
- `src/atlas_agent/__init__.py`: 0.5.7.dev31

## Safety

- Provider execution remains disabled.
- No provider calls are made.
- No API keys are read.
- No network is used.
- No provider SDKs are imported.
- No credentials are loaded.
- No `.env.atlas` is loaded.
- No `os.environ` lookup is attempted.
- No broker/live/execution behavior changed.
- No approvals or pending orders are created.
- No trading signals generated.
- No live trading authorization.
- Boundary diff is clean under `src/atlas_agent/config`, `brokers`, `execution`, `safety`, `risk`.
