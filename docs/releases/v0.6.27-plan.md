# v0.6.27 Release Plan

Status: **released**.

- Status: released
- Current public release: `v0.6.27`
- Previous public release: `v0.6.26`
- Next planned release: `v0.6.28`
- Release date: 2026-07-30
- Source/package version: `0.6.27`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

`v0.6.27` is the current public GitHub release. `v0.6.26` is the historical
previous public release. The release is GitHub-only; the annotated tag and
GitHub Release are created as part of the cutover and are recorded as not yet
created here.

Release notes: [v0.6.27 Release Notes](v0.6.27.md). Trust status:
[v0.6.27 Trust and Release Status](../trust/v0.6.27-status.md).

## Candidates

Three candidates are released in `v0.6.27`. Three remain proposed and carry
forward to `v0.6.28`.

| id | title | status | safety notes |
|---|---|---|---|
| CAND-032 | Deferred domain loading for CLI startup | proposed | Import-timing and packaging only. Changes no gate, default, or runtime decision; loads no credentials; adds no network access. |
| CAND-033 | Wire the declared risk limits that no rule reads | released | Tightens the deterministic risk gate and enables nothing. Can only reject orders accepted today. |
| CAND-034 | Map the research error codes the CLI flattens | proposed | Widens a vetted status allowlist. Enables nothing; the fail-safe generic fallback stays. |
| CAND-035 | Absorb the repeated research handler preamble | released | CLI error handling and workspace resolution only. Adds and removes no gate. |
| CAND-036 | L3 bounded live autonomy: explicit opt-in and its path | proposed | Design only; authorizes nothing. Retains per-order human approval. L4 still requires external review. |
| CAND-037 | Absorb the per-artifact helper copies verified identical | released | Validator and artifact-loader plumbing. No gate added or removed. The one behavioural change refuses more. |

## Possible future candidates

These are directions only. None are implemented, designed, or accepted, and
listing them here authorizes no runtime, safety, broker, provider, credential,
network, or trading behavior. Each would require its own design, implementation
plan, review, and acceptance before landing in a future release line.

The three carried forward are described in
[v0.6.27 Release Candidates](v0.6.27-candidates.md) with the measurements behind
them, and are re-proposed against `v0.6.28`.

## Safety boundaries

`v0.6.27` changed protected runtime boundaries — `safety/`, `brokers/`,
`execution/`, `risk/` and `config/` — because that is where the audited defects
were. Every change is in the refusing direction, and the per-change direction is
tabulated in [v0.6.27 Trust and Release Status](../trust/v0.6.27-status.md).

Nothing in this line widens what the system may do. Specifically, no candidate or
document in `v0.6.27`:

- enables live trading or makes live trading a default;
- enables live order submission or changes `can_submit`;
- enables broker or provider execution, or calls real brokers or providers;
- loads broker/provider credentials;
- adds network access;
- places, cancels, or flattens orders, or creates pending orders;
- mutates approval queues, the kill switch, heartbeat, deadman, or the audit
  hash-chain;
- broadens the CAND-014 provider-artifact extraction boundary.

`atlas run --mode live` remains fail-closed. This is a GitHub-only project and
PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.27` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

## Not financial advice

Atlas Agent is a software tool, not a financial advisor. Trading involves
significant risk of loss. This plan makes no profitability, trading-safety,
live-readiness, production-readiness, or unattended-safe-operation claim.
