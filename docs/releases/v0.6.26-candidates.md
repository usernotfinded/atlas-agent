# v0.6.26 Release Candidates

Status: **planning**. `v0.6.26` is the next planned release line and is not
released. `v0.6.25` is the current public GitHub release. `v0.6.24` is the
historical previous public release. No `v0.6.26` tag, GitHub Release, or PyPI
publication exists.

- Status: planning
- Current public release: `v0.6.25`
- Previous public release: `v0.6.24`
- Next planned release: `v0.6.26`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

## Candidate acceptance

No candidates are currently accepted or proposed for the `v0.6.26` candidate
chain. `v0.6.26` remains planning-only and is not released. Acceptance of any
future candidate is documentation/governance/test-only and does not authorize
live trading, live submit, order placement, broker/provider execution, credential
loading, network access, or approval queue mutation.

## Accepted

No candidates are currently accepted for `v0.6.26`.

## Proposed

No additional candidates are currently proposed for `v0.6.26`.

## Safety boundaries

No `v0.6.26` planning content enables live trading, live submit, broker/provider
execution, credential loading, network access, order placement, pending-order
creation, or approval queue mutation, and none changes `RiskManager`,
kill-switch, deadman, heartbeat, or audit hash-chain behavior. The CAND-014
provider-artifact extraction boundary remains strictly at
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
`atlas run --mode live` remains fail-closed. PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.26` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.
