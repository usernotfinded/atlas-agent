# v0.6.26 Release Candidates

Status: **released**. `v0.6.26` is the current public GitHub release. `v0.6.25`
is the historical previous public release. `v0.6.27` is the next planned release
line.

- Status: released
- Current public release: `v0.6.26`
- Previous public release: `v0.6.25`
- Next planned release: `v0.6.27`
- Release date: 2026-07-13
- PyPI: not published
- Tag created: yes
- GitHub Release: created

## Candidate acceptance

One candidate, **CAND-021 — Metadata-driven current-state test fixtures**, is
**released** as part of `v0.6.26` on 2026-07-13. Acceptance and release of
CAND-021 is documentation/governance/test-only and does not authorize live
trading, live submit, order placement, broker/provider execution, credential
loading, network access, or approval queue mutation.

## Released

- **CAND-021 — Metadata-driven current-state test fixtures** — released on
  2026-07-13 as part of `v0.6.26`.
  - Adds a shared `release_identity` pytest fixture in `tests/conftest.py` that
    loads current release identity from `docs/releases/release-metadata.json`.
  - Refactors current-state tests to use fixture values (`source_version`,
    `current_public_release`, `next_planned_release`, `previous_public_release`)
    instead of hardcoded `0.6.25` / `v0.6.25` / `v0.6.26` / `v0.6.24` literals.
  - Test-only; no runtime, safety, broker, provider, credential, network, or
    trading changes.

## Proposed

No additional candidates are proposed for `v0.6.26`. The `v0.6.26` release
cutover has completed; no further `v0.6.26` candidate acceptance is possible.

## Safety boundaries

No `v0.6.26` content enables live trading, live submit, broker/provider
execution, credential loading, network access, order placement, pending-order
creation, or approval queue mutation, and none changes `RiskManager`,
kill-switch, deadman, heartbeat, or audit hash-chain behavior. The CAND-014
provider-artifact extraction boundary remains strictly at
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
`atlas run --mode live` remains fail-closed. PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.26` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.
