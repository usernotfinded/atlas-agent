# v0.6.23 Release Candidates

Status: **planning**. `v0.6.23` is the next planned release line and is not
released. `v0.6.22` is the current public GitHub release. `v0.6.21` is the
historical previous public release. No `v0.6.23` tag, GitHub Release, or PyPI
publication exists.

- Status: planning
- Current public release: `v0.6.22`
- Previous public release: `v0.6.21`
- Next planned release: `v0.6.23`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

## Candidate acceptance

**CAND-017** is accepted into the `v0.6.23` candidate chain as a
release-maintenance tooling candidate. Accepting a candidate here is
documentation/governance only and does not authorize live trading, live submit,
order placement, broker/provider execution, credential loading, network access,
or approval queue mutation.

## Accepted

- **CAND-017: Post-Release CI Run-ID Recorder**
  - Adds `scripts/update_release_assurance_ci.py` and tests.
  - Queries GitHub Actions for core workflow runs associated with a release tag.
  - Updates `docs/releases/v0.6.X-post-release-assurance.md` and `.json` with
    run IDs and URLs.
  - Dry-run by default; requires `--write` to mutate files.
  - Uses the authenticated `gh` CLI and Python standard library only.
  - Does not enable live trading, live submit, broker/provider execution,
    credential loading, network access, order placement, pending-order creation,
    or approval queue mutation.
  - Does not change `RiskManager`, kill switch, deadman, heartbeat, or audit
    hash-chain behavior.
  - Does not broaden the CAND-014 provider-artifact extraction boundary.

## Proposed

No additional candidates are currently proposed for `v0.6.23`. See
[v0.6.23-plan.md](v0.6.23-plan.md) for possible future directions, none of which
are implemented or accepted.

## Safety boundaries

No `v0.6.23` planning content enables live trading, live submit, broker/provider
execution, credential loading, network access, order placement, pending-order
creation, or approval queue mutation, and none changes `RiskManager`,
kill-switch, deadman, heartbeat, or audit hash-chain behavior. The CAND-014
provider-artifact extraction boundary remains strictly at
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
`atlas run --mode live` remains fail-closed. PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.23` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.
