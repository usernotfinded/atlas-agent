# Release v0.5.7.dev46 — README Quickstart Verification Pack

## Scope

Batch 9.9 — README Quickstart Verification Pack.

This batch makes the README quickstart and public onboarding flow verifiable, safe, and reproducible. It ensures a new developer can understand and run Atlas Agent in a safe local/paper/sandbox mode without touching brokers, credentials, network, provider execution, or live trading.

## What changed

- **README quickstart cleanup**: Reorganized the `## Quickstart` section into clear, copy-paste-friendly steps (Install → Create workspace → Validate → Backtest → Inspect safety dossiers). Added explicit "What is intentionally disabled" subsection. Required safety wording added: sandbox-only, paper-first, offline-safe, live trading disabled by default, not financial advice, no broker/order path, no credentials loaded.
- **Quickstart verification script**: Added `scripts/verify_readme_quickstart.py` to programmatically verify that README quickstart commands are safe and consistent with the repo. Checks for forbidden command patterns, forbidden claims, required safe phrases, required safe commands, forbidden fragments, and secret-like placeholders.
- **Quickstart tests**: Added `tests/test_readme_quickstart_verification.py` (21 tests) covering README structure, safety wording, forbidden claims, forbidden fragments, command path safety, and verification script behavior.
- **Version bump**: `0.5.7.dev45` → `0.5.7.dev46`.

## What did not change

- No new trading behavior.
- No broker integrations.
- Risk gates remain unchanged.
- No execution logic changes.
- No config loading changes.
- No live trading enablement.
- No credential loading.
- No network calls.
- No OS open/file-launch commands.
- No HTML/PDF export.
- No dashboard features.
- No new artifact type.
- Protected boundaries (`config/`, `brokers/`, `execution/`, `safety/`, `risk/`) remain untouched.

## Safety invariants (unchanged)

The following remain `false` everywhere:

- `provider_call_allowed`
- `actual_provider_call_made`
- `provider_response_trusted`
- `mock_response_trusted`
- `trading_signal_generated`
- `approval_created`
- `pending_order_created`
- `broker_touched`
- `network_enabled`
- `credentials_loaded`
- `trust_upgrade_performed`
- `trust_decision_granted`
- `provider_execution_unlocked`
- `real_provider_response_imported`
- `live_trading_path_enabled`
- `broker_order_path_enabled`

## Validation

- `check_version_consistency.py` passes.
- `check_forbidden_claims.py` passes.
- `verify_readme_quickstart.py` passes.
- `pytest tests/test_readme_quickstart_verification.py` passes.
- `pytest tests/test_provider_safety_dossier_docs.py` passes.
- `pytest tests/test_provider_policy_docs.py` passes.
- `release_check.sh --full` passes.
- Protected boundary diff is empty.
