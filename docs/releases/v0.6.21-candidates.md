# v0.6.21 Release Candidates

Status: **released**. `v0.6.21` is the current public GitHub release. `v0.6.20`
is the previous public release. `v0.6.19` and earlier are historical releases.
The `v0.6.21` tag and GitHub Release are created; PyPI is not published.

- Status: released
- Current public release: `v0.6.21`
- Previous public release: `v0.6.20`
- Next planned release: `v0.6.22`
- PyPI: not published
- Tag created: yes
- GitHub Release: created

## Candidate acceptance

**CAND-013** is released as part of `v0.6.21` with acceptance verdict
`PASS_WITH_WARNINGS` on 2026-07-04. This acceptance does not authorize live
trading, live submit, order placement, broker/provider execution, credential
loading, network access, or approval queue mutation.

**CAND-014 Phase 2** is released as part of `v0.6.21` with acceptance verdict
`PASS` on 2026-07-06. The accepted scope is a one-module pilot extraction that
moves shared artifact mechanics for
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py` behind a
minimal `src/atlas_agent/research/artifact_engine.py` while preserving Phase 1
golden compatibility. Phase 1 remains accepted. Extraction beyond this single
pilot module remains not authorized and not started. This acceptance does not
authorize live trading, live submit, order placement, broker/provider execution,
credential loading, network access, or approval queue mutation.

## Accepted

- **CAND-013:** Public/Trust Docs Drift Coverage Guard.
  - Extends `scripts/check_public_docs_consistency.py` to use release-metadata as
    the current/next-planned authority.
  - Catches stale trust README `(current public)` labels on non-current
    releases.
  - Catches autonomy-roadmap candidate-state contradictions against next-planned
    candidate-chain JSON (a no-op until a next-planned candidate chain records
    accepted/released candidates).
  - Verifies the README status line matches the current public release and warns
    on orphaned release notes not referenced in `CHANGELOG.md`.
  - Adds `tests/test_public_docs_consistency.py` coverage (`52 passed`) using
    diagnostic-substring assertions rather than brittle full-output matching.
  - Documents the new coverage in `docs/development/checks-reference.md` and
    corrects one autonomy-roadmap current/historical/planning line
    (`v0.6.20` current public; `v0.6.19` and earlier historical; `v0.6.21`
    planning-only).
  - Preserves exit codes `0` (pass) and `1` (violations/errors); introduces no
    new exit code. Remains deterministic, local, and standard-library only, with
    no network access and no credential loading.
  - Changes no runtime, safety, CLI, broker, provider, version, or
    release-metadata code.
  - Design doc: `docs/public-trust-docs-drift-coverage-guard-design.md`
  - Implementation plan: `docs/public-trust-docs-drift-coverage-guard-implementation-plan.md`
  - Design commit: `9b59147f7ef9c73fb12645e7e20152f02810bf38`
  - Design fixup commit: `a5c973c22caeb0df1dcfc88e2da80c56fb0c31cc`
  - Implementation-plan commit: `0975c817efa5fcbeaec12a179a9256d03a093ebb`
  - Implementation commit: `a43975df430a3ad0f053601da044e384566a0b59`
  - Acceptance commit: this document is updated by the CAND-013 acceptance commit.
  - Design, implementation-plan, and implementation work are complete; the
    `v0.6.21` release is available on GitHub only; PyPI is not published.
  - Design review verdict: `PASS`
  - Implementation-plan review verdict: `PASS`
  - Implementation review verdict: `PASS_WITH_WARNINGS`
  - Acceptance verdict: `PASS_WITH_WARNINGS`

- **CAND-014:** Provider Artifact Engine Deduplication - Phase 2 pilot
  extraction only.
  - Phase 1 accepted scope: Provider Artifact Inventory + Golden
    Characterization Tests.
  - Phase 2 accepted scope: one-module pilot extraction only.
  - Delivered `src/atlas_agent/research/artifact_engine.py`, a 66-line minimal
    pilot-driven module providing `ArtifactSpec` plus helpers for canonical
    SHA-256 hashing, JSON load/save, artifact path construction, and artifact
    discovery.
  - Migrated `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`
    to delegate artifact mechanics to `artifact_engine.py` while preserving all
    public functions, signatures, version constants, schema fields, status values,
    hash exclusions, `validate_provider_id` behavior, disabled-provider behavior,
    replay/summarize/doctor structures, and CLI-facing behavior.
  - No spec module was created.
  - The implementation commit changed only the two files above; no tests,
    fixtures, CLI, checker, docs/releases, changelog, version, safety, broker, or
    other provider files were modified.
  - Existing golden characterization tests passed (`13 passed`).
  - Existing final-safety-seal and upstream mock-response chain tests passed
    (`299 passed`) during independent review.
  - Heavy gates `dev_check.sh`, `ci_check.sh`, and `release_check.sh --quick`
    passed during independent review.
  - Pilot module:
    `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
  - New engine module: `src/atlas_agent/research/artifact_engine.py`.
  - Design doc: `docs/provider-artifact-engine-deduplication-design.md`
  - Implementation plan:
    `docs/provider-artifact-engine-deduplication-implementation-plan.md`
  - Phase 2 plan doc:
    `docs/provider-artifact-engine-deduplication-phase-2-plan.md`
  - Inventory doc: `docs/provider-artifact-engine-deduplication-inventory.md`
  - Design commit: `d610dfcdd98bae31916d9914450543607b7e69ac`
  - Implementation-plan commit: `bc07ac1758215ca41075a0eac6dada95e5a1390d`
  - Phase 1 implementation commit:
    `49092649c718081d282b0d5d02983a0e286fbc39`
  - Phase 1 acceptance commit:
    `d02e88dcd42b552d48abf3a2b9f36a63254ac7fe`
  - Phase 2 plan commit: `9cb234356405d78da8d7f27ffe35da4b31884aa0`
  - Phase 2 implementation commit:
    `0390d4461bd5bb7d0015d3af2079cfca6ca6309d`
  - Acceptance commit: this document is updated by the CAND-014 Phase 2
    acceptance commit.
  - Design, implementation-plan, Phase 1, and Phase 2 pilot implementation work
    are complete.
  - Artifact-engine extraction status: pilot complete; extraction beyond the
    pilot module is not authorized.
  - Release status: released as part of `v0.6.21`; available on GitHub only;
    PyPI is not published.
  - Phase 2 implementation review verdict: `PASS`
  - Phase 2 acceptance verdict: `PASS`

- **CAND-015:** Bounded Live Autonomy Readiness Gate.
  - Adds an evidence-only, simulated-only L2/L3 autonomy readiness gate.
  - New engine module: `src/atlas_agent/agent/bounded_live_autonomy_readiness.py`.
  - New CLI module: `src/atlas_agent/agent/bounded_live_autonomy_readiness_cli.py`.
  - Registers `atlas agent bounded-live-readiness` configlessly via
    `src/atlas_agent/cli_bootstrap.py` and as a delegated legacy subparser command.
  - Adds `scripts/check_bounded_live_autonomy_readiness_contract.py` static
    contract checker and corresponding tests.
  - Adds sample fixtures under
    `tests/fixtures/bounded_live_autonomy_readiness/`.
  - Consumes CAND-004/CAND-005/CAND-006/CAND-007/CAND-008 artifacts plus
    CAND-015-owned policy fixtures and evaluates a 15-gate fail-closed sequence.
  - Records `bounded-live-readiness.json` and `bounded-live-readiness-report.md`
    only when all gates pass.
  - Does not submit orders, call brokers/providers, load credentials, mutate
    runtime state, or claim live readiness.
  - Design doc: `docs/bounded-live-autonomy-readiness-design.md`
  - Operator doc: `docs/bounded-live-autonomy-readiness.md`
  - Implementation commit: this document is updated by the CAND-015 acceptance commit.
  - Design status: complete.
  - Implementation status: complete.
  - Release status: released as part of `v0.6.21`; available on GitHub only;
    PyPI is not published.
  - Implementation review verdict: `PASS`
  - Acceptance verdict: `PASS`

**CAND-015** is released as part of `v0.6.21` with acceptance verdict `PASS` on
2026-07-06. It adds an evidence-only, simulated-only L2/L3 autonomy readiness
gate (`atlas agent bounded-live-readiness`) that consumes upstream
CAND-004/CAND-005/CAND-006/CAND-007/CAND-008 artifacts plus CAND-015-owned local
policy fixtures and evaluates a strict 15-gate fail-closed sequence. It records
`bounded-live-readiness.json` and `bounded-live-readiness-report.md` only when
all gates pass. It does not submit orders, call brokers/providers, load
credentials, mutate runtime state, or claim live readiness. `v0.6.21` is released
on GitHub only; PyPI is not published.

## Proposed

No additional candidates beyond CAND-013, CAND-014 Phase 2, and CAND-015 are
currently proposed for `v0.6.21`.

## Known warnings (non-blocking)

1. False-positive-mitigation asymmetry: the inline current-public block in
   `_check_stale_release_status_lines` does not honor `historical_context` while
   an adjacent labeled/inline public-claim block does. This has no impact on the
   current repository and may be acceptable intended strictness, but it is a
   future hardening candidate. Non-blocking.
2. Methodological live-mode verification note: `atlas run --mode live` was
   verified via a safe isolated paper/null-provider workspace rather than against
   the local live config with credentials. This preserved review boundaries and
   is acceptable because CAND-013 changed no runtime, safety, CLI, broker,
   provider, or credential-loading code. Non-blocking.
3. Historical candidate-chain docs for pre-`v0.6.21` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata.
4. CAND-014 inventory-count warning: the Phase 1 inventory doc records that the
   broad provider-related test-file command returned 87 files, while a later
   live filesystem command returned 120 because it included ignored
   `__pycache__` entries plus current Phase 1 files. The Phase 1 source
   inventory remains correct: 22 `provider_*.py` modules and 28,908 source
   lines. Non-blocking.
5. CAND-014 strict pilot boundary: extraction beyond
   `provider_mock_response_final_safety_seal.py` remains not authorized. Any
   further extraction requires a separate planning/review step. Non-blocking.
6. CAND-014 ID classification: PASS with a non-blocking legacy reuse warning.
   `CAND-014` is the correct current-epoch next ID for `v0.6.21` after
   CAND-013. The older `v0.6.12` `CAND-014` belongs to the legacy release-local
   epoch, and the candidate-chain checker enforces uniqueness within each JSON
   file rather than globally.
7. CAND-015 status semantics: `bounded_live_readiness_recorded` is
   evidence-recording status only. It is not live readiness, not trading safety,
   not profitability evidence, not permission to trade, and not authorization to
   submit orders. L3 bounded live autonomy remains a future research concept.
   Non-blocking.
8. CAND-015 live-mode verification note: `atlas run --mode live` is verified via
   a safe isolated paper/null-provider workspace rather than against a local live
   config with credentials, because CAND-015 changes no runtime, safety, CLI,
   broker, provider, or credential-loading code. Non-blocking.
