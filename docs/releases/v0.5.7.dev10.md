# Atlas Agent v0.5.7.dev10

## Summary

- This is a development tag.
- Focus: research provider interface and provider-boundary documentation.
- It is not production live-trading readiness.
- No financial outcome is promised.

## Highlights

- Research provider interface (`ResearchProvider` protocol).
- Deterministic/local provider as the only enabled default.
- Disabled/fail-closed LLM provider stub.
- Unsupported providers fail closed; no silent fallback.
- Provider docs-truth tests.
- No real LLM, API, or network behavior is enabled.

## Research Provider Boundary

- `deterministic` is the enabled and default provider.
- Unsupported providers fail closed.
- External/LLM providers are not enabled in this tag.
- The disabled LLM stub does not call APIs.
- No API keys are read by the research provider layer.
- No broker credentials are required.
- Provider selection does not authorize live trading.
- Provider output remains paper-only and analysis-only.

## Research Workflow State

Available commands:

- `atlas research run --symbol AAPL`
- `atlas research list`
- `atlas research show RUN_ID`
- `atlas research plan RUN_ID`
- `atlas research verify PLAN_ID`
- `atlas research evaluate PLAN_ID --data PATH`
- `atlas research summary`
- `atlas research check-artifacts`
- `atlas research timeline`
- `./scripts/demo_research_workflow.sh`

State:

- Paper-only.
- Analysis-only.
- Local artifacts.
- Local CSV data for evaluation.
- No broker submit.
- No pending orders.
- No approvals.
- No live-trading authorization.

## Safety and Compatibility Notes

- Live submit remains disabled by default and gated.
- Research commands are not trading advice.
- Evaluation does not produce trading signals.
- Evaluation does not estimate profit.
- Research commands do not generate live orders.
- `check-artifacts` and `timeline` are local inspection commands only.
- Reconcile remains read-only.
- Safe quote gate remains execution-time only.

## Validation

Validation commands run:

- `python3.11 scripts/check_version_consistency.py`
- `python3.11 scripts/check_forbidden_claims.py`
- `python3.11 -m pytest -q`
- `python3.11 -m pip check`
- `./scripts/demo_paper_workflow.sh`
- `./scripts/demo_research_workflow.sh`
- `git diff --check`
- `./scripts/release_check.sh`
- `python3.11 scripts/check_no_protected_staged.py`

Results:

- Version consistency passed for 0.5.7.dev10.
- Forbidden-claim scan passed.
- Full pytest passed.
- pip check passed.
- Demo paper workflow passed.
- Demo research workflow passed.
- release_check.sh passed.
- Protected-staged check passed.

## Known Limitations

- Development tag, not production live-trading readiness.
- Research workflow is deterministic/local for now.
- No LLM research provider is enabled in this tag.
- Real broker E2E/sandbox verification is not completed.
- Package smoke may require network/build dependencies depending on mode.
- Offline package smoke requires a Python where `python -m build` works.
