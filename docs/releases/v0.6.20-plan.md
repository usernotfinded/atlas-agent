# v0.6.20 Release Plan

Status: **planning**.

- Current public release: `v0.6.19`
- Next planned release: `v0.6.20`
- PyPI: not published
- Tag created: false
- GitHub Release: not created

`v0.6.19` is the current public GitHub release. `v0.6.18` is the historical
previous public release. The source/package version is `0.6.19`. The `v0.6.19`
annotated tag and GitHub Release exist as a GitHub-only release. `v0.6.20` is
the next planned release line. No `v0.6.20` tag, GitHub Release, or PyPI
publication exists.

## Candidates under consideration

| id | title | status | safety notes |
|---|---|---|---|
| CAND-012 | Candidate-Chain Consistency Guard | accepted into v0.6.20 | Deterministic local read-only checker for release-metadata / candidate-chain JSON / Markdown consistency; candidate-chain-specific forbidden-claim guard; stdlib-only, no network, no credentials, no runtime imports; integrated into dev/CI and checks-reference docs. No live trading, live submit, broker/provider execution, credential loading, network access, or order placement. No safety-module changes. |

## Acceptance

CAND-012 is accepted into the `v0.6.20` candidate chain with acceptance verdict
`PASS_WITH_WARNINGS` on 2026-07-01. The acceptance does not authorize live
trading, live submit, order placement, broker/provider execution, credential
loading, network access, or approval queue mutation. No release cutover has been
performed.

## Known warnings (non-blocking)

`bash scripts/ci_check.sh` fails at step 9 (`check_public_docs_consistency.py`)
due to stale public-release claims where `v0.6.17`/`v0.6.18` are called
current/latest in docs unrelated to CAND-012:

- `docs/public-repo-hygiene.md`
- `docs/public-launch-messaging.md`
- `docs/public-faq.md`
- `docs/autonomy-roadmap.md`

This failure is pre-existing and was reproduced on the implementation-plan
baseline `484690ca88d55e2ef70da9ed90545122d088a199`. It does not block CAND-012
acceptance, but it must be remediated before any `v0.6.20` release cutover.
