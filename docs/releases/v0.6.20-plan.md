# v0.6.20 Release Plan

Status: **released** as `v0.6.20` on 2026-07-02.

- Current public release: `v0.6.20`
- Previous public release: `v0.6.19`
- Next planned release: `v0.6.21`
- PyPI: not published
- Tag created: true
- GitHub Release: created

`v0.6.20` is the current public GitHub release. `v0.6.19` is the historical
previous public release. The source/package version is `0.6.20`. The `v0.6.20`
annotated tag and GitHub Release exist as a GitHub-only release. `v0.6.21` is
the next planned release line. No `v0.6.21` tag, GitHub Release, or PyPI
publication exists.

## Candidates under consideration

| id | title | status | safety notes |
|---|---|---|---|
| CAND-012 | Candidate-Chain Consistency Guard | released in v0.6.20 | Deterministic local read-only checker for release-metadata / candidate-chain JSON / Markdown consistency; candidate-chain-specific forbidden-claim guard; stdlib-only, no network, no credentials, no runtime imports; integrated into dev/CI and checks-reference docs. No live trading, live submit, broker/provider execution, credential loading, network access, or order placement. No safety-module changes. |

## Acceptance

CAND-012 is accepted into the `v0.6.20` candidate chain with acceptance verdict
`PASS_WITH_WARNINGS` on 2026-07-01 and is released in `v0.6.20`. The acceptance
does not authorize live trading, live submit, order placement, broker/provider
execution, credential loading, network access, or approval queue mutation. The
`v0.6.20` release cutover was performed after a release-readiness review and
remediation of the public-docs drift noted in the acceptance record.

## Known warnings (non-blocking)

1. A full `pytest` run or full `bash scripts/release_check.sh` may exceed local
   interactive timeouts. The `--quick` gate and all targeted checks pass.
2. Historical candidate-chain docs for pre-`v0.6.20` releases still emit
   expected warnings from `check_candidate_chain.py` because they reflect
   historical planning state rather than current metadata.
