# v0.6.24 Release Candidates

Status: **planning**. `v0.6.24` is the next planned release line and is not
released. `v0.6.23` is the current public GitHub release. `v0.6.22` is the
historical previous public release. No `v0.6.24` tag, GitHub Release, or PyPI
publication exists.

- Status: planning
- Current public release: `v0.6.23`
- Previous public release: `v0.6.22`
- Next planned release: `v0.6.24`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

## Candidate acceptance

**CAND-018** is accepted into the `v0.6.24` candidate chain as a
release-maintenance drift-hardening candidate. Accepting a candidate here is
documentation/governance only and does not authorize live trading, live submit,
order placement, broker/provider execution, credential loading, network access,
or approval queue mutation.

## Accepted

- **CAND-018: Hardcoded Release-Literal Sweep**
  - Adds `scripts/check_hardcoded_release_literals.py` and tests.
  - Scans active scripts for string literals matching the current source version,
    current public tag, or next planned tag from `release-metadata.json`.
  - Flags literals used in comparisons, assignments, and collections so
    release-maintenance drift is caught before cutover.
  - Excludes intentionally pinned historical release-specific checkers
    (`check_v06*`, `check_v05*`).
  - Fixes the same class of hardcoded version-literal drift in
    `scripts/check_autonomous_paper_workflow_demo.py` and
    `scripts/check_paper_provider_isolation.py` that had already been fixed in
    `scripts/check_bounded_autonomy_governance.py` during the v0.6.23
    post-release pass.
  - Uses only the Python standard library, performs no network access, loads no
    credentials, and does not mutate repository files.
  - Does not enable live trading, live submit, broker/provider execution,
    credential loading, network access, order placement, pending-order creation,
    or approval queue mutation.
  - Does not change `RiskManager`, kill switch, deadman, heartbeat, or audit
    hash-chain behavior.
  - Does not broaden the CAND-014 provider-artifact extraction boundary.

## Proposed

No additional candidates are currently proposed for `v0.6.24`. See
[v0.6.24-plan.md](v0.6.24-plan.md) for possible future directions, none of which
are implemented or accepted.

## Safety boundaries

No `v0.6.24` planning content enables live trading, live submit, broker/provider
execution, credential loading, network access, order placement, pending-order
creation, or approval queue mutation, and none changes `RiskManager`,
kill-switch, deadman, heartbeat, or audit hash-chain behavior. The CAND-014
provider-artifact extraction boundary remains strictly at
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
`atlas run --mode live` remains fail-closed. PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.24` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.
