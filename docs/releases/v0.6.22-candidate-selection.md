# v0.6.22 Candidate Selection Gate

> **Status:** planning (`v0.6.22` next planned release line; not released)
> **Current public release:** `v0.6.21`
> **Previous public release:** `v0.6.20`
> **Next planned release:** `v0.6.22`
> **PyPI:** not published
> **Tag created:** no
> **GitHub Release:** not created
> **Not financial advice.** Atlas Agent is a software tool, not a financial
> advisor. Trading involves significant risk of loss.

`v0.6.21` is the current public GitHub release. `v0.6.20` is the historical
previous public release. The `v0.6.22` candidate chain is open. **CAND-016** is
**accepted** into the `v0.6.22` candidate chain (acceptance verdict **PASS**, not
released). Opening this planning line and accepting CAND-016 authorizes no live
trading, live submit, order placement, broker execution, provider execution,
credential loading, network access, or approval queue mutation.

Current public release: `v0.6.21`. Source/package version: `0.6.21`. PyPI is not
published. `v0.6.22` is planning-only; no `v0.6.22` tag, GitHub Release, or PyPI
publication exists.

## Accepted candidates

- **CAND-016: Release-Maintenance Drift Hardening** — accepted on 2026-07-13 with
  verdict **PASS** (not released). A docs/checker/test-only candidate that makes
  the release-maintenance path metadata-driven: it adds a reverse-drift roadmap
  guard (an already-released candidate must not be listed under the next-planned
  planning line) plus deterministic tests, and retains the metadata-driven
  release-assurance demo default. It changes no runtime, safety, CLI, broker,
  provider, credential, version, or release-metadata behavior and does not
  broaden the CAND-014 boundary. Design and plan:
  [docs/cand-016-release-maintenance-drift-hardening.md](../cand-016-release-maintenance-drift-hardening.md).

### Why CAND-016 is eligible

- It is a docs/checker/test-only addition; it does not implement, authorize, or
  enable any trading or execution path.
- It closes a concrete release-maintenance drift class observed after the
  `v0.6.21` cutover, using release metadata as the authority rather than
  hardcoded release literals.
- It uses only the Python standard library, performs no network access, loads no
  credentials, and does not mutate repository state at runtime.
- It preserves the deliberate per-release version-pin tripwire assertions and the
  CAND-014 single-module extraction boundary.
- It ships deterministic, offline tests and preserves the checker's `0`/`1` exit
  codes.

## Proposed candidates

No candidates are currently proposed for `v0.6.22`.

Further possible directions are listed in
[v0.6.22-plan.md](v0.6.22-plan.md); none beyond CAND-016 are designed,
implemented, or accepted.

## Selection rules

Any future `v0.6.22` candidate must satisfy the repository's candidate selection
rules before acceptance:

1. No live trading enablement — fail-closed defaults unchanged.
2. No broker/provider execution enablement — no direct AI-to-broker path.
3. No secrets or credentials committed.
4. No unsafe marketing claims — no promises of specific returns, eliminated
   risk, live readiness, or autonomous trading readiness.
5. Protected runtime boundaries preserved — `RiskManager`, approval queues, kill
   switch, heartbeat, deadman, audit hash-chain, live-submit opt-in, and
   `can_submit` are not bypassed.
6. Small, reviewable candidate batches that are independently valuable.
7. Deterministic test and documentation coverage.
8. Green verification before acceptance.
9. The CAND-014 provider-artifact extraction boundary is not broadened beyond
   `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.22` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

This candidate selection gate is documentation/governance only. The `v0.6.22`
release cutover is a separate future step and has not started.
