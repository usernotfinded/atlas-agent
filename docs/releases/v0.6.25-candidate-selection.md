# v0.6.25 Candidate Selection Gate

> **Status:** released (`v0.6.25` current public release)
> **Current public release:** `v0.6.25`
> **Previous public release:** `v0.6.24`
> **Next planned release:** `v0.6.26`
> **Release date:** 2026-07-13
> **PyPI:** not published
> **Tag created:** yes
> **GitHub Release:** created
> **Not financial advice.** Atlas Agent is a software tool, not a financial
> advisor. Trading involves significant risk of loss.

`v0.6.25` is the current public GitHub release. `v0.6.24` is the historical
previous public release. Two candidates, **CAND-019 — Docs/config hardcoded
release-literal sweep** and **CAND-020 — Post-v0.6.24 test drift sweep**, have
been proposed, accepted, and released as part of `v0.6.25` on 2026-07-13.
Opening this planning line, accepting, and releasing CAND-019 and CAND-020
authorizes no live trading, live submit, order placement, broker execution,
provider execution, credential loading, network access, or approval queue
mutation.

Current public release: `v0.6.25`. Source/package version: `0.6.25`. PyPI is not
published. The `v0.6.25` tag and GitHub Release exist.

## Released candidates

- **CAND-019 — Docs/config hardcoded release-literal sweep** — released on
  2026-07-13 as part of `v0.6.25`. Extends
  `scripts/check_hardcoded_release_literals.py` to scan `configs/`, `examples/`,
  and `docs/examples/` for monitored release-identity literals matching
  `release-metadata.json`, and adds regression tests. Static checker/test-only;
  no runtime, safety, broker, provider, credential, network, or trading changes.

- **CAND-020 — Post-v0.6.24 test drift sweep** — released on 2026-07-13 as part
  of `v0.6.25`. Updates stale post-`v0.6.24` test fixtures and assertions to the
  current release identity, adds `0.6.24` to `SECURITY.md`, and records this
  candidate. Test-only drift fixes; no runtime, safety, broker, provider,
  credential, network, or trading changes.

### Why CAND-019 and CAND-020 were eligible

- They are checker/test-only additions; they do not implement, authorize, or
  enable any trading or execution path.
- They close concrete release-maintenance gaps observed after the `v0.6.24`
  cutover: example configs and docs can embed release identity literals that
  become stale, and post-release test fixtures can drift from the current release
  identity.
- They use only the Python standard library, perform no network mutation, load no
  credentials, and do not mutate repository files.
- They preserve the CAND-014 single-module extraction boundary.
- They ship deterministic, offline tests.

## Proposed candidates

No candidates are currently proposed for `v0.6.26`.

Further possible directions are listed in
[v0.6.25-plan.md](v0.6.25-plan.md); none beyond CAND-019 and CAND-020 were
designed, implemented, or accepted for `v0.6.25`.

## Selection rules

Any future `v0.6.26` candidate must satisfy the repository's candidate selection
rules before acceptance:

1. No live trading enablement — fail-closed defaults unchanged.
2. No broker/provider execution enablement — no direct AI-to-broker path.
3. No secrets or credentials committed.
4. No unsafe marketing claims — no promises of specific returns, eliminated
   risk, live readiness, or autonomous trading readiness.
5. Protected runtime boundaries preserved — `RiskManager`, approval queues, kill
   switch, heartbeat, deadman, audit hash-chain, live-submit opt-in, and
   `can_submit` are not bypassed.
6. Small, reviewable candidate batches that are independently valuable.
7. Deterministic test and documentation coverage.
8. Green verification before acceptance.
9. The CAND-014 provider-artifact extraction boundary is not broadened beyond
   `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.25` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

This candidate selection gate is documentation/governance only. The `v0.6.25`
release cutover has completed; no further `v0.6.25` candidate acceptance is
possible.
