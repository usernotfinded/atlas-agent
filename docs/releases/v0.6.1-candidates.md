# v0.6.1 Patch Candidate Selection

> **Status:** Candidate selection complete — awaiting implementation batch
> **Last updated:** 2026-06-05
> **Not financial advice.** Atlas Agent is a software tool, not a financial advisor. Trading involves significant risk of loss.

## Scope

This document selects safe, small, backward-compatible maintenance items for a future `v0.6.1` patch release. It does **not** implement them. It does **not** bump the version. It does **not** create a release.

## Selection criteria

A candidate is eligible for `v0.6.1` only if **all** of the following are true:

1. **Small and safe** — changes are docs-only, checker-only, test-only, or CI-only.
2. **Backward compatible** — no API changes, no CLI contract changes, no breaking behavior.
3. **No runtime trading impact** — does not modify broker, execution, safety, risk, or provider defaults.
4. **No new features** — patch scope per Semantic Versioning 2.0.0: backward-compatible bug fixes only.
5. **Verified by evidence** — the issue is reproducible from current repo state, CI logs, or checker output.
6. **Testable** — the fix can be covered by existing or new tests without network or credentials.

Candidates that fail any criterion are rejected or deferred to a later minor release.

## Candidate table

| id | title | category | priority | source/evidence | risk | expected files | acceptance criteria | selected for v0.6.1 | reason |
|---|---|---|---|---|---|---|---|---|---|
| CAND-001 | Update v0.6.1-plan.md post-release status and resolved issues | docs | P1 | `docs/releases/v0.6.1-plan.md` still states "All post-release verification checks pass except `check_v060_readiness.py`"; this was fixed in `a4ede9c` and CI run `27038772473` is green. | low | `docs/releases/v0.6.1-plan.md` | Doc accurately reflects current post-release state; known issues section moved to resolved. **Implemented in Batch 52.** | **yes** | Outdated planning doc is misleading to operators. |
| CAND-002 | Fix capability inventory version section labeling | docs | P1 | `docs/v0.6-capability-inventory.md` labels the strategy pack as "v0.6.1 — Backtesting Strategy Pack", but the pack shipped in `v0.6.0`. | low | `docs/v0.6-capability-inventory.md` | Section header corrected to reflect v0.6.0 release contents; no runtime changes. **Implemented in Batch 52.** | **yes** | Misleading version labeling confuses patch scope. |
| CAND-003 | Add post-release readiness check to CI quick-gate | CI/test | P1 | CI workflow `ci.yml` does not exercise `check_v060_readiness.py --post-release`; the checker passes locally (`valid: true` with zero errors/warnings). | low | `.github/workflows/ci.yml`, `tests/test_ci_workflows.py`, `scripts/check_v060_readiness.py` | Post-release readiness step present in CI `quick-gate` job; test asserts its presence; auth-flaky `gh` failures warn instead of error. **Implemented in Batch 52.** | **yes** | Ensures post-release validation runs on every push, preventing regressions. |
| CAND-004 | Align GitHub release body trailing newline | docs | P2 | `docs/releases/v0.6.0.md` and the published GitHub release body differ by exactly one trailing newline. | none | `docs/releases/v0.6.0.md` (if edited) or `gh release edit` | Content is byte-identical except benign trailing whitespace. | **no** | Benign formatting; no user impact. Defer to next doc-edit batch. |
| CAND-005 | Checks-reference.md cross-link polish | docs | P2 | `docs/development/checks-reference.md` could reference `scripts/check_v061_candidates.py` if added. | none | `docs/development/checks-reference.md` | Cross-links between planning checkers, readiness checks, and long-running check guidance. **Implemented in Batch 52.** | **yes** | Low-cost docs consistency. |
| CAND-006 | Runtime diagnostics timeout guidance refinement | docs/checker | P2 | `scripts/check_runtime_diagnostics.py` and `docs/development/checks-reference.md` already document timeout triage. Could add expected ranges for `research_check.sh` on slower machines. | none | `scripts/check_runtime_diagnostics.py`, `docs/development/checks-reference.md` | Guidance covers slow-machine estimates without weakening checks. | **no** | Already sufficient; defer unless user reports confusion. |

## Accepted candidates

### CAND-001 — Update v0.6.1-plan.md post-release status
- **What:** Refresh `docs/releases/v0.6.1-plan.md` to reflect that the post-release readiness checker now works, CI is green, and known issues #1 and #2 are resolved.
- **Why:** A planning doc with false negatives misleads operators assessing patch urgency.
- **Files:** `docs/releases/v0.6.1-plan.md`
- **Safety:** Docs-only. No runtime impact.
- **Status:** Implemented in Batch 52.

### CAND-002 — Fix capability inventory version section labeling
- **What:** Change the "v0.6.1 — Backtesting Strategy Pack" header in `docs/v0.6-capability-inventory.md` to accurately reflect that the strategy pack shipped in v0.6.0.
- **Why:** Incorrect version labels create confusion about what is and is not in a patch.
- **Files:** `docs/v0.6-capability-inventory.md`
- **Safety:** Docs-only. No runtime impact.
- **Status:** Implemented in Batch 52.

### CAND-003 — Add post-release readiness check to CI quick-gate
- **What:** Add `python3.11 scripts/check_v060_readiness.py --post-release` to the CI `quick-gate` job in `.github/workflows/ci.yml`, plus matching tests in `tests/test_ci_workflows.py`. Narrowly update `scripts/check_v060_readiness.py` so that `gh` authentication failures warn instead of error in post-release mode.
- **Why:** The post-release checker currently passes locally but is not exercised in CI; a regression would only be caught manually.
- **Files:** `.github/workflows/ci.yml`, `tests/test_ci_workflows.py`, `scripts/check_v060_readiness.py`
- **Safety:** CI-only. Read-only checker. No network, no credentials, no runtime trading impact.
- **Status:** Implemented in Batch 52.

### CAND-005 — Checks-reference.md cross-link polish
- **What:** Add cross-links between planning checkers, readiness checks, and long-running check guidance in `docs/development/checks-reference.md`.
- **Why:** Keeps the checks reference complete as new checkers are introduced.
- **Files:** `docs/development/checks-reference.md`
- **Safety:** Docs-only. No runtime impact.
- **Status:** Implemented in Batch 52.

## Rejected / deferred candidates

| id | title | reason |
|---|---|---|
| CAND-004 | Align GitHub release body trailing newline | Benign formatting mismatch with zero user impact. Not worth a patch cutover. |
| CAND-006 | Runtime diagnostics timeout guidance refinement | Already documented sufficiently. Defer until operator feedback indicates confusion. |
| REJ-001 | New broker adapters (Binance completion, CCXT enablement, IBKR implementation) | New features, not bug fixes. Defer to v0.6.2 or later minor release. |
| REJ-002 | Provider execution unlock or real LLM integration | Runtime-sensitive, safety-critical. Explicitly out of patch scope. |
| REJ-003 | PyPI publish | Not a bug fix. Requires separate approval and process. |
| REJ-004 | Dashboard frontend rewrite | Large, new-feature work. Defer to minor release. |
| REJ-005 | New strategy packs | New features. Defer to minor release. |
| REJ-006 | Autonomous trading or skill auto-activation | Violates safety boundaries. Never acceptable in a patch. |

## Safety boundaries

The following must remain unchanged in any `v0.6.1` maintenance release:

- `src/atlas_agent/config/` — no new live defaults.
- `src/atlas_agent/brokers/` — no new submit paths, no execution enabled by default.
- `src/atlas_agent/execution/` — no direct AI-to-broker paths.
- `src/atlas_agent/safety/` — no kill switch bypasses.
- `src/atlas_agent/risk/` — risk limits must not be weakened.
- No version bump to `0.6.1` in this batch.
- No `docs/releases/v0.6.1.md` release notes file created yet.
- No tag, GitHub release, or PyPI publish.

## Test / release criteria

Before any `v0.6.1` cutover:

1. All accepted candidates are implemented in a separate implementation batch.
2. `dev_check.sh`, `ci_check.sh`, and `release_check.sh --quick` pass.
3. CI is green on `main`.
4. Protected boundary diff is clean.
5. Dangerous pattern scan is clean.
6. No new runtime trading behavior introduced.

## Non-goals

- No new runtime features for `v0.6.1`.
- No new broker adapters.
- No new provider integrations.
- No live trading default changes.
- Risk gates must remain unchanged.
- No approval queue or kill switch changes.
- No automatic skill activation or learning execution.
- No PyPI publish in this batch.

## Next-step sequence

1. **Implementation batch** — implement CAND-001 through CAND-005 in a single focused batch.
2. **Verification batch** — run full gates, verify CI green, verify protected boundary diff clean.
3. **Version-prep batch** (only if justified) — bump to `0.6.1`, create `docs/releases/v0.6.1.md`, generate release assurance.
4. **Release batch** (only after explicit owner approval) — tag `v0.6.1`, create GitHub release.

---

*This document was generated as part of Batch 51: v0.6.1 patch candidate selection. It is planning-only and does not modify version, runtime behavior, or release state.*
