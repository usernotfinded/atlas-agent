# v0.6.22 Release Plan

Status: **planning**. `v0.6.22` is the next planned release line and is not
released.

- Status: planning
- Current public release: `v0.6.21`
- Previous public release: `v0.6.20`
- Next planned release: `v0.6.22`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

`v0.6.21` is the current public GitHub release. `v0.6.20` is the historical
previous public release. The source/package version is `0.6.21`. `v0.6.22` is
the next planned release line; no `v0.6.22` tag, GitHub Release, or PyPI
publication exists, and the v0.6.22 release cutover has not started.

The `v0.6.21` release state, CI status, and post-release checks are recorded in
[v0.6.21 Post-Release Assurance](v0.6.21-post-release-assurance.md).

## Candidates under consideration

No candidates are accepted or proposed for `v0.6.22` yet. This planning line is
open with an empty candidate chain.

| id | title | status | safety notes |
|---|---|---|---|
| — | — | none accepted | No candidate work is authorized beyond planning skeletons and safety/test/documentation guardrails. |

## Possible future candidates

These are directions only. None are implemented, designed, or accepted, and
listing them here authorizes no runtime, safety, broker, provider, credential,
network, or trading behavior. Each would require its own design, implementation
plan, review, and acceptance before landing.

- Regression coverage for the release-line tag guards in
  `scripts/check_bounded_autonomy_governance.py`,
  `scripts/check_autonomous_paper_workflow_demo.py`, and
  `scripts/check_paper_provider_isolation.py`, so the "next planned line not
  tagged locally" guard stays synchronized with `next_planned_release` in
  release metadata (see the post-release hardening note below).
- Documentation-only hardening for the `_check_stale_release_status_lines`
  `historical_context` asymmetry recorded in the v0.6.21 known warnings.
- Continued provider-artifact deduplication planning, keeping the CAND-014
  extraction boundary strictly at
  `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`; no
  broadening is authorized.

## Post-release hardening landed in this planning pass

- Added focused regression coverage proving the next-planned tag guards in the
  three checkers above track `next_planned_release` from release metadata rather
  than a hardcoded prior planning tag. This closes the class of bug found during
  the v0.6.21 cutover, where one checker's guard still referenced `v0.6.21`
  after the metadata next-planned line advanced to `v0.6.22`. Test-only; no
  runtime, safety, broker, provider, credential, or CLI behavior changed.

## Safety boundaries

This planning line enables nothing at runtime. Specifically, no candidate or
document in `v0.6.22` planning:

- enables live trading or makes live trading a default;
- enables live order submission or changes `can_submit`;
- enables broker or provider execution, or calls real brokers or providers;
- loads broker/provider credentials;
- adds network access;
- places, cancels, or flattens orders, or creates pending orders;
- mutates approval queues, the kill switch, heartbeat, deadman, or the audit
  hash-chain;
- broadens the CAND-014 provider-artifact extraction boundary.

`atlas run --mode live` remains fail-closed. This is a GitHub-only project and
PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.22` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

## Not financial advice

Atlas Agent is a software tool, not a financial advisor. Trading involves
significant risk of loss. This plan makes no profitability, trading-safety,
live-readiness, production-readiness, or unattended-safe-operation claim.
