# v0.6.5 Patch Candidate Selection

> **Not financial advice.** Atlas Agent is a software tool, not a financial advisor. Trading involves significant risk of loss.

> **Status:** planning — candidate selection only

`v0.6.4` is the current public GitHub release (tagged and published). `v0.6.5` is **not**
version-prepared, **not** tagged, and **not** released. This document is a planning artifact
that selects safe maintenance candidates for a future `v0.6.5` patch release. It does **not**
authorize implementation, version bumps, tag creation, GitHub release creation, or PyPI
publishing. `docs/releases/v0.6.5.md` does not exist yet and must not be created in this batch.

## Selection criteria

A candidate is eligible for `v0.6.5` only if **all** of the following are true:

- It is a small, backward-compatible fix or docs improvement.
- It does not change runtime trading, broker, provider, risk, approval, or kill-switch behavior.
- It does not add new runtime features, broker adapters, or provider integrations.
- It does not require new secrets, credentials, or network calls.
- It is safe to implement directly on `main` without a feature branch.
- It does not weaken existing checks or safety boundaries.

## Candidates

| id | title | category | priority | source/evidence | risk | expected files | acceptance criteria | selected for v0.6.5 | reason |
|---|---|---|---|---|---|---|---|---|---|
| CAND-001 | Update trust center and public docs to reflect `v0.6.4` as current public release | docs consistency | P1 | `docs/trust/README.md`, `docs/security/release-readiness.md`, `docs/public-launch-readiness.md`, and `docs/trust/v0.6.4-status.md` still describe v0.6.4 as "not yet tagged" or v0.6.3 as current public release. | low | `docs/trust/README.md`, `docs/security/release-readiness.md`, `docs/public-launch-readiness.md`, `docs/trust/v0.6.4-status.md` | All public-release references resolve to `v0.6.4` as current; stale "not yet tagged" wording is removed. | **yes — implemented in Batch 68** | Low-cost docs consistency that prevents reviewer confusion. |
| CAND-002 | Normalize/fix stale `PUBLIC_TAG` and package/tag version references in checkers | checker metadata | P1 | `scripts/check_version_consistency.py`, `scripts/check_trust_center.py`, `scripts/check_public_launch_readiness.py`, and related tests still hardcode `v0.6.3` as the current public tag even though `v0.6.4` is released. | low | `scripts/check_version_consistency.py`, `scripts/check_trust_center.py`, `scripts/check_public_launch_readiness.py`, `scripts/check_public_docs_consistency.py`, `scripts/check_stable_release_decision.py`, `scripts/check_package_distribution.py`, `scripts/main_health.py`, `tests/` | All checker constants reference `v0.6.4` as current public tag and `0.6.4` as package version; stale `v0.6.3` current-release claims are rejected by checkers. | **yes — implemented in Batch 68** | Prevents recurring post-release test failures and checker drift. |
| CAND-003 | Regression test for `security_md_current` package-version validation | test coverage | P1 | `f3972ef` fixed `release_assurance.py` but has no dedicated regression test for the package-version vs tag-version distinction. | none | `tests/test_release_assurance.py` | Test verifies that `security_md_current` passes when `SECURITY.md` contains the package version and fails when it contains only a mismatched tag version. | **yes** | Protects the fix from regression. |
| CAND-004 | Update release checklist version references | docs consistency | P2 | `docs/release-checklist.md` contains example commands with hardcoded historical versions (e.g., `v0.6.3` in tag examples) that drift after each release. | none | `docs/release-checklist.md` | Examples reference the current source version and public tag without stale historical values. | **yes** | Low-cost docs hygiene. |
| CAND-005 | Harden deterministic ordering in JSON outputs | CI determinism | P2 | Some evidence or report generators may still use unsorted collections when producing JSON, causing flaky cross-platform diffs. | low | Relevant research/report modules and tests | All JSON-producing report modules use deterministic ordering (`sort_keys=True` or explicit sorting). | **yes** | Reduces CI noise without adding skips. |
| CAND-006 | `v0.6.5` release prep checker skeleton | checker infrastructure | P1 | Following the pattern of `check_v064_release_prep.py`, prepare a skeleton that validates absence of `v0.6.5` artifacts while source version remains `0.6.4`. | none | `scripts/check_v065_release_prep.py`, `tests/test_v065_release_prep.py` | Planning mode rejects `docs/releases/v0.6.5.md` and `0.6.5` version bump; release-prep mode will accept them after bump. | **yes** | Prepares the release-prep gate without enabling it prematurely. |

## Deferred candidates

| id | title | reason |
|---|---|---|
| DEF-001 | New broker adapters | New features, not maintenance. Defer to v0.7.0 or later. |
| DEF-002 | New strategy packs | New features, not maintenance. Defer to v0.7.0 or later. |
| DEF-003 | Dashboard rewrite | Out of patch scope. Requires design review. |
| DEF-004 | Major architecture refactor | Out of patch scope and too risky. |
| DEF-005 | Research-to-live trading bridge | Safety-sensitive. Requires explicit gated design. |

## Rejected items

| id | title | reason |
|---|---|---|
| REJ-001 | Live trading default changes | Safety boundary. Never changed in a patch. |
| REJ-002 | Broker execution enablement | Safety boundary. Never changed in a patch. |
| REJ-003 | Provider execution unlock | Safety boundary. Never changed in a patch. |
| REJ-004 | Autonomous trading | Not claimed or enabled. |
| REJ-005 | PyPI publish | Not approved in this planning batch; requires separate process. |
| REJ-006 | Automatic skill activation | Skill candidates remain manually reviewed and offline. |
| REJ-007 | Automatic learning execution | Learning suggestions remain offline and manually reviewed. |

## Safety boundaries

- Live trading remains disabled by default.
- Live submit remains disabled by default.
- Provider execution remains disabled by default.
- Broker execution remains disabled by default.
- Risk gates remain deterministic and unchanged.
- Approval queue behavior remains unchanged.
- Kill-switch behavior remains unchanged.
- Human approval remains required for live order flow.
- Credentials are not loaded unless explicitly configured.
- No provider APIs, broker APIs, or real webhooks are called from CI or local checks.

## Constraints

- ❌ Version is **not** bumped to `0.6.5` in this batch.
- ❌ `docs/releases/v0.6.5.md` release notes file does **not** exist yet.
- ❌ No tag, GitHub release, or PyPI publish yet.
- ❌ No runtime trading behavior changes.

## Release criteria

Before `v0.6.5` can be prepared or tagged, the following must be true:

- [ ] Accepted candidates are implemented.
- [ ] `dev_check`, `ci_check`, and `release_check --quick` pass locally.
- [ ] CI on `main` is green after the prep commit is pushed.
- [ ] Version is bumped to `0.6.5` in `pyproject.toml` and `src/atlas_agent/__init__.py`.
- [ ] `docs/releases/v0.6.5.md` and `docs/trust/v0.6.5-status.md` are created.
- [ ] `CHANGELOG.md` has a `[0.6.5]` entry.
- [ ] `scripts/check_v065_release_prep.py` and matching tests are created.
- [ ] Protected boundary diff is empty.
- [ ] Forbidden claims scan is clean.
- [ ] Owner approval is obtained for tag cutover.

## Next steps

1. Obtain owner approval for the selected candidate list.
2. Implement accepted candidates in small, reviewable batches.
3. Run required local checks after each batch.
4. When all accepted candidates are complete, bump version to `0.6.5` and create
   release prep artifacts (`docs/releases/v0.6.5.md`,
   `docs/trust/v0.6.5-status.md`, `scripts/check_v065_release_prep.py`,
   `tests/test_v065_release_prep.py`).
5. Run full release prep verification.
6. Obtain owner approval before tag creation.
7. Create annotated tag `v0.6.5` and GitHub release only after explicit approval.

---

*This candidate selection document was created on 2026-06-07 as part of Batch 67:
v0.6.5 patch candidate selection after the `v0.6.4` release. It is planning-only
and does not modify version, runtime behavior, or release state.*
