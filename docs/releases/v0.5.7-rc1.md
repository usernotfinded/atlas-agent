# Atlas Agent v0.5.7-rc1

> **Not financial advice.** Atlas Agent is a software tool, not a financial advisor. Trading involves significant risk of loss.

## Overview

This is the first release candidate for the v0.5.7 line.

`v0.5.7-rc1` is a **sandbox/paper/preflight release candidate**. It is not a live-trading release. It does not enable provider execution, broker order submission, or live trading.

## Scope

- Release engineering cutover from `0.5.7.dev50` to `0.5.7rc1` (package) / `v0.5.7-rc1` (public tag).
- CHANGELOG synchronized through dev50.
- Provider safety chain completed (mock response simulation through provider safety dossier).
- Public docs and quickstart verified.
- Release candidate readiness and cutover dry-run checks completed.
- No runtime behavior changes in this cutover.
- No broker/execution/risk/config/safety boundary changes.

## What this is not

- Not a live-trading ready release.
- Not a production trading milestone.
- Not a provider execution unlock.
- Not a broker integration milestone.
- Not a trust grant.
- Not a profitability or trading-correctness claim.
- Not financial advice.

## Safety posture

- Live trading disabled by default.
- Provider execution remains locked.
- Trust remains blocked.
- No broker/order path in the provider safety workflow.
- No credentials loaded by the provider safety workflow.
- No network enabled by the provider safety workflow.

## Version mapping

| Source | Version |
|---|---|
| Python package | `0.5.7rc1` |
| Git tag / GitHub release | `v0.5.7-rc1` |

## Validation checklist

- `python3.11 scripts/check_version_consistency.py` — version consistency OK.
- `python3.11 scripts/check_forbidden_claims.py` — forbidden claims scan clean.
- `python3.11 scripts/verify_readme_quickstart.py` — README quickstart verification PASSED.
- `python3.11 scripts/check_public_docs_consistency.py` — public docs consistency check PASSED.
- `python3.11 -m pytest tests/test_changelog_consistency.py -q` — CHANGELOG consistency tests pass.
- `python3.11 -m pytest tests/test_rc1_cutover_consistency.py -q` — RC1 cutover consistency tests pass (if present).
- Protected boundaries clean under `src/atlas_agent/config`, `brokers`, `execution`, `safety`, `risk`.
- No package artifacts staged.

## Staging reminders

- Do not use `git add .`.
- Stage explicit files only.
- Do not stage `.atlas/`, `.env`, `.env.atlas`, `memory/`, `build/`, `dist/`, `*.egg-info/`, generated reports, credentials, local logs, scratch files.
- Do not tag, push, or publish in this batch.

## Manual post-review commands (do not run automatically)

After external review and explicit PUSH OK:

```bash
git tag -a v0.5.7-rc1 -m "Atlas Agent v0.5.7-rc1"
git push origin v0.5.7-rc1
```
