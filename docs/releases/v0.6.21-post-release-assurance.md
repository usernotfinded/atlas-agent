# v0.6.21 Post-Release Assurance

> **Release:** `v0.6.21`
> **Source/package version:** `0.6.21`
> **Status:** current public GitHub-only release
> **PyPI:** not published
> **Not financial advice.** Atlas Agent is a software tool, not a financial
> advisor. Trading involves significant risk of loss.

This document is the deterministic public assurance record for the
owner-authorized `v0.6.21` GitHub-only release, captured after the cutover. It
records the verified release state, GitHub Actions/CI status, and the local
checks re-run during post-release stabilization. `v0.6.20` is the historical
previous public release and `v0.6.22` is the next planning line. This is a
GitHub-only release record; it is **not** a live-trading, production-readiness,
or unattended-safe-operation claim.

## Release State

| Field | Value |
|---|---|
| Current public release | `v0.6.21` |
| Source/package version | `0.6.21` |
| Previous public release | `v0.6.20` |
| Next planned release | `v0.6.22` |
| Release type | GitHub-only |
| Release commit | `df5f0ef1993d7b3cf0455f49fc077d665dc7bfdb` |
| Annotated tag | `v0.6.21` (dereferences to the release commit) |
| GitHub Release | `v0.6.21` (not draft, not prerelease, latest) |
| GitHub Release assets | 0 (no uploaded artifacts) |
| PyPI | unpublished |

## Tag and GitHub Release Verification

- The annotated tag `v0.6.21` exists locally and on `origin` and dereferences to
  the release commit `df5f0ef1993d7b3cf0455f49fc077d665dc7bfdb`
  (`git ls-remote --tags origin 'v0.6.21^{}'`).
- The GitHub Release `v0.6.21` exists, is not a draft, is not a prerelease, is
  marked latest, targets `main`, and has 0 attached assets:
  <https://github.com/usernotfinded/atlas-agent/releases/tag/v0.6.21>.
- No new tag or GitHub Release was created by this post-release pass.

## GitHub Actions / CI Status

CI is green on the release commit `df5f0ef`:

| Workflow | Run | Conclusion |
|---|---|---|
| CI (`core-functional`, `quick-gate`) | [28886368182](https://github.com/usernotfinded/atlas-agent/actions/runs/28886368182) | success |
| Release Gate | [28886390864](https://github.com/usernotfinded/atlas-agent/actions/runs/28886390864) | success |
| Atlas Agent Paper Routines (scheduled) | 28892284529 | success |

CI failures visible on commits before the cutover reflect the pre-cutover stale
public-release drift that `v0.6.21` remediated; they are resolved on the release
commit.

## Local Checks Re-Run (Post-Release Stabilization)

All of the following were re-run locally and passed during this pass:

- `check_version_consistency`, `check_release_metadata`, `check_candidate_chain`,
  `check_public_docs_consistency`, `check_forbidden_claims`,
  `check_bounded_autonomy_governance`, `check_trust_center`,
  `check_onboarding_docs`, `check_public_launch_readiness`,
  `check_bounded_live_autonomy_readiness_contract`.
- `tests/test_provider_artifact_golden_contracts.py`, the CAND-015
  bounded-live-autonomy-readiness suites, `tests/test_public_docs_consistency.py`,
  `tests/test_candidate_chain.py`, `tests/test_cli_command_compatibility.py`.
- `tests/test_next_planned_tag_guard.py` (new regression coverage for the
  next-planned tag guard).
- `scripts/dev_check.sh`, `scripts/ci_check.sh`, `scripts/release_check.sh --quick`.

## PyPI and Artifacts

- PyPI remains **not published**; `pypi_published` is `false` in release metadata.
- No `dist/`, `build/`, or `*.egg-info/` artifacts are tracked or uploaded.
- The GitHub Release carries 0 assets.

## Safety Boundaries (Unchanged)

This release and this post-release pass change no runtime trading, broker,
provider, credential, or safety behavior:

- Live trading and live submit remain disabled by default; `atlas run --mode live`
  remains fail-closed.
- Provider and broker execution defaults are unchanged and disabled; no real
  brokers or providers are called and no credentials are loaded.
- `RiskManager`, approval queues, kill switch, heartbeat, deadman, the audit
  hash-chain, live-submit opt-in, and `can_submit` are not bypassed or changed.
- The CAND-014 provider-artifact extraction boundary remains strictly at
  `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.

## Remaining Warnings and Caveats

1. **CAND-013** acceptance verdict is `PASS_WITH_WARNINGS`; the
   [v0.6.21 final readiness audit](v0.6.21-final-readiness-audit.md) records the
   known non-blocking warnings.
2. **CAND-015** retains a clean-room independence caveat: implementation and
   implementation-review were performed by the same agent system, so a
   clean-room independent review remains outstanding. No external or human
   review evidence is claimed or invented.
3. **CAND-014 Phase 2** is a strict one-module pilot extraction; no extraction
   beyond `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`
   is authorized.

## Statement

`v0.6.21` is a GitHub-only governance/documentation/safety-check release. This
assurance record confirms release identity and process integrity only. It makes
no profitability, trading-safety, live-readiness, production-readiness, or
unattended-safe-operation claim.
