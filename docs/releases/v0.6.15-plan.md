# v0.6.15 Release Plan

Status: planning-only.

`v0.6.15` is the next planning line after the GitHub-only `v0.6.14` release.
No candidate, source/package bump, tag, GitHub Release, or PyPI publication is
authorized by this planning seed.

No features are selected.
