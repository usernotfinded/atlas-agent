# v0.5.8.1 Release Notes

`v0.5.8.1` is a packaging/install hotfix for `v0.5.8`.

## Fixed

- Fixed packaged runtime templates for `atlas init --template routine-trader`.
- `routine-trader` templates now ship as package resources under `src/atlas_agent/templates`.
- Workspace template resolution now uses `importlib.resources`, with a source-checkout fallback for local development.

## Verification

- Clean wheel install outside the source repository is verified.
- Wheel and sdist artifact checks cover packaged `routine-trader` templates.
- Editable-install clean checks cover `atlas init --template routine-trader`.

## Safety

- No live trading changes were made.
- No provider execution changes were made.
- No broker execution changes were made.
- No protected runtime boundaries were changed.
- No PyPI publish was performed in this task.
- No GitHub Release was created in this task.

## Disclaimer

Atlas Agent is software, not financial advice. This hotfix does not make profitability claims, does not guarantee trading outcomes, and does not change the risk profile of live trading.
