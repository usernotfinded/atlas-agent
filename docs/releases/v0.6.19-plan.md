# v0.6.19 Release Plan

Status: **planning-only / not released**.

`v0.6.19` is the next planned release line. `v0.6.18` is the current public
GitHub release. The source/package version is `0.6.18`. No `v0.6.19` annotated
tag or GitHub Release exists.

## Candidates under consideration

| id | title | status | safety notes |
|---|---|---|---|
| CAND-011 | Kill-Switch `last_heartbeat()` Type-Safety Cleanup | accepted in planning | Narrows audit payload construction in `kill_switch.py` to eliminate a pre-existing mypy `union-attr` warning; preserves `last_heartbeat` key and `str | None` value shape; adds mandatory `heartbeat_expired` audit-payload regression tests. No live trading, live submit, broker/provider execution, credential loading, network access, or order placement. No decision logic or fail-closed behavior change. |

## Acceptance

CAND-011 is accepted into the `v0.6.19` candidate chain with acceptance verdict
`PASS` on 2026-07-01. No `v0.6.19` release cutover has occurred. The acceptance
does not authorize live trading, live submit, order placement, broker/provider
execution, credential loading, network access, or approval queue mutation.
