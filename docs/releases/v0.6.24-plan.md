# v0.6.24 Release Plan

Status: **planning**. `v0.6.24` is the next planned release line and is not
released.

- Status: planning
- Current public release: `v0.6.23`
- Previous public release: `v0.6.22`
- Next planned release: `v0.6.24`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

`v0.6.23` is the current public GitHub release. `v0.6.22` is the historical
previous public release. The source/package version is `0.6.23`. `v0.6.24` is
the next planned release line; no `v0.6.24` tag, GitHub Release, or PyPI
publication exists, and the v0.6.24 release cutover has not started.

The `v0.6.23` release state, CI status, and post-release checks are recorded in
[v0.6.23 Post-Release Assurance](v0.6.23-post-release-assurance.md) once
available.

## Candidates under consideration

One candidate, **CAND-018**, is accepted for `v0.6.24`. No other candidates are
accepted or proposed yet.

| id | title | status | safety notes |
|---|---|---|---|
| CAND-018 | Hardcoded Release-Literal Sweep | accepted | Static checker/test-only; scans scripts for hardcoded release literals; no runtime, safety, broker, provider, credential, or trading changes. |

## Possible future candidates

These are directions only. None are implemented, designed, or accepted, and
listing them here authorizes no runtime, safety, broker, provider, credential,
network, or trading behavior. Each would require its own design, implementation
plan, review, and acceptance before landing.

None are currently proposed for `v0.6.24`.

## Safety boundaries

This planning line enables nothing at runtime. Specifically, no candidate or
document in `v0.6.24` planning:

- enables live trading or makes live trading a default;
- enables live order submission or changes `can_submit`;
- enables broker or provider execution, or calls real brokers or providers;
- loads broker/provider credentials;
- adds network access;
- places, cancels, or flattens orders, or creates pending orders;
- mutates approval queues, the kill switch, heartbeat, deadman, or the audit
  hash-chain;
- broadens the CAND-014 provider-artifact extraction boundary.

`atlas run --mode live` remains fail-closed. This is a GitHub-only project and
PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.24` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

## Not financial advice

Atlas Agent is a software tool, not a financial advisor. Trading involves
significant risk of loss. This plan makes no profitability, trading-safety,
live-readiness, production-readiness, or unattended-safe-operation claim.
