{
  "release_line": "v0.6.28",
  "status": "planning",
  "source_version": "0.6.27",
  "current_public_release": "v0.6.27",
  "next_planned_release": "v0.6.28",
  "release_date": null,
  "pypi_published": false,
  "tag_created": false,
  "github_release_created": false,
  "candidates": [
    {
      "id": "CAND-032",
      "title": "Deferred domain loading for CLI startup",
      "status": "proposed",
      "accepted": false,
      "category": "performance",
      "priority": "P2",
      "risk": "medium",
      "source": "Every atlas invocation costs ~0.50 s before it does anything: ~346 ms importing atlas_agent.cli, ~75 ms building the argparse tree, ~12 ms interpreter startup. The suite spawns the CLI from 97 call sites, and the dev gate's slowest step spends 73% of its wall clock in 21 such subprocesses, so this is primarily a developer-loop cost rather than a UX one.",
      "expected_files": [
        "src/atlas_agent/cli.py",
        "src/atlas_agent/cli_commands/",
        "src/atlas_agent/config/schema.py"
      ],
      "acceptance_criteria": "A measured reduction in atlas --version wall time with the command surface unchanged: scripts/check_cli_command_compatibility.py passes, every trust-contract envelope is byte-identical, and a test pins the reduction so it cannot silently regress.",
      "safety_notes": "Import-timing and packaging only. Changes no gate, no default, and no runtime decision: not RiskManager, approval queues, kill switch, heartbeat, deadman, the audit hash-chain, live-submit opt-in, or can_submit. Loads no credentials and adds no network access. The cli_bootstrap.py pre-router keeps its four configless commands and its stdlib-only module scope.",
      "open_question": "Two levers, priced in docs/development/safety-invariant-audit-followups.md. ConfigDict(defer_build=True) measures 74% off pydantic class creation (200 synthetic models: 178 ms to 46.5 ms), about a quarter of startup, but 128 of 129 model classes inherit BaseModel directly with no shared base, and it moves a malformed model from an import-time error to a first-use error. Lazy per-domain handler import has a larger ceiling (config alone imports in 137 ms against 273 ms for cli_commands) but threads local imports through a 5.9k-line dispatch whose command surface is a pinned trust contract. Deciding which, or neither, is the candidate."
    },
    {
      "id": "CAND-034",
      "title": "Map the research error codes the CLI flattens to a generic message",
      "status": "proposed",
      "accepted": false,
      "category": "usability",
      "priority": "P2",
      "risk": "low",
      "source": "safe_research_session_error maps a ResearchSessionError to a vetted status and falls back to ('research_error', 'Research command failed.') for anything unrecognised. src/atlas_agent raises 205 distinct literal codes and RESEARCH_SESSION_ERROR_CODES maps 69 of them, so at least 136 land on the generic message. Measured over the CLI, 30 of the 175 atlas research subcommands answer a nonexistent id with 'Research command failed.' while the reason was known, static, and safe to show \u2014 for example provider_safety_dossier_source_seal_missing. A further 33 raise sites build their code at runtime and are not counted, so 136 is a lower bound.",
      "expected_files": [
        "src/atlas_agent/research/errors.py",
        "tests/research/test_research_error_code_mapping.py"
      ],
      "acceptance_criteria": "Entries added for the codes an operator can reach, the ratchet in tests/research/test_research_error_code_mapping.py lowered to the new count, and no status string carrying a path, identifier, or any value from the exception rather than a literal.",
      "safety_notes": "The generic fallback stays. It is the fail-safe half of this design \u2014 an unmapped exception string must never be echoed, because in general it can carry a path or user data. This only widens the vetted allowlist, so it enables nothing at runtime and changes no gate, default, or trading behaviour.",
      "open_question": "Resolved by precedent. The 148 already-mapped codes use only 32 distinct messages, and three artifact families mapped independently converged on the identical five-slot pattern: four shared messages (identifier, malformed, unsupported schema, not found) plus one sentence naming the artifact. Applying it to the 136 unmapped codes: 21 reuse a shared message and 115 take their family's sentence, so the vocabulary decision is 13 sentences, not 136. safe_research_session_error must stay an exact dict lookup -- a runtime derivation rule would echo the 33 codes that are built at runtime. Blast radius measured: 18 research_error assertions across 5 test files. This is now implementation, not decision, and stays proposed only because widening 136 machine-readable statuses is a contract change that should land as its own reviewed unit."
    },
    {
      "id": "CAND-036",
      "title": "L3 bounded live autonomy: explicit opt-in and the path that consults it",
      "status": "proposed",
      "accepted": false,
      "category": "autonomy",
      "priority": "P1",
      "risk": "high",
      "source": "The ladder defines L3 as requiring per-order human approval, strict RiskManager limits, explicit opt-in, and active operator oversight. Three of the four now hold: approval queues exist and OrderRouter allowlists exactly manual_live; the three declared risk limits that evaluated nothing are enforced as of CAND-033; and the deadman is verified to block a hung cycle with its composition pinned. The fourth is missing \u2014 live submit has an opt-in bound to a config fingerprint, autonomy has none.",
      "expected_files": [
        "docs/l3-bounded-live-autonomy-design.md",
        "src/atlas_agent/safety/",
        "src/atlas_agent/agent/"
      ],
      "acceptance_criteria": "The opt-in and the path that consults it land together: an autonomy opt-in record with its own fingerprint over the enforced limits, refused when absent, invalidated when any of those limits change, and a fail-closed L3 path that records a heartbeat. Per-order human approval is retained \u2014 a version without it is L4 and out of scope. Paper-validated before any live configuration is possible.",
      "safety_notes": "Design only at this stage; this candidate authorizes nothing. L3 is not unattended trading \u2014 per-order human approval is part of its definition. L4 remains out of reach without external legal, security, operational, risk, broker-adapter, and regulatory review by qualified parties, which the governance document requires to be done by them and not by self-assessment.",
      "open_question": "Three, recorded in the design. Whether the L3 path must record a heartbeat explicitly, since an absent heartbeat reads as fresh and the deadman is inert on any path that evaluates without recording. What 'active operator oversight' means as a signal \u2014 a deadman proves the agent is alive, not that a human is watching. And how narrow the first cut should be; the ladder says tightly bounded, and one symbol, one side, one broker, paper-validated first is what makes the review worth doing."
    },
    {
      "id": "CAND-038",
      "title": "Three admission rules share the name validate_provider_id",
      "status": "released",
      "accepted": true,
      "category": "maintainability",
      "priority": "P2",
      "risk": "medium",
      "source": "CAND-037 collapsed six helper families and stopped at the seventh. validate_provider_id has 21 copies that look like duplicates and are not: normalising the error code away leaves three disjoint admission rules under one name. 15 copies accept the four disabled external provider ids and reject 'mock'; 5 accept only 'mock'; 1 accepts both. 'mock' is not among the disabled ids, so the first two accept strictly different sets. The union belongs to provider_mock_response_simulation, which calls the function on two different fields -- its upstream preview's provider id, which is external, and its own artifact's, which must be 'mock' -- and re-checks provider_id != 'mock' immediately after validating its own. A twenty-second copy lives in providers/provider_preflight.py and is not a copy at all: a bounded-string check raising PreflightValidationError, in a different package for a different purpose. It is out of scope and left alone, but it means the name carries four meanings across the tree, not three.",
      "expected_files": [
        "src/atlas_agent/research/sandbox_contracts.py",
        "src/atlas_agent/research/provider_mock_response_simulation.py"
      ],
      "acceptance_criteria": "Three named policies replacing one overloaded name, with the module needing both saying which it means at each call site, and the strict re-check that currently defends the union either preserved or made unnecessary by the rename. No admission set changes.",
      "safety_notes": "A rename of validators, not a change to what they admit. Any change to an accepted set would be a policy change and is out of scope. No gate added or removed; no live trading, live submit, broker or provider execution, credential loading, or network access.",
      "open_question": "Resolved. The union is gone: provider_mock_response_simulation names validate_source_provider_id for the upstream preview's external provider and validate_mock_provider_id for its own artifact, so the strict != 'mock' re-check that defended the union is no longer load-bearing and is removed. No admitted set changed, asserted per module over the whole provider universe rather than argued. The providers/provider_preflight.py function keeps the name in its own package.",
      "acceptance_verdict": "PASS"
    }
  ]
}
