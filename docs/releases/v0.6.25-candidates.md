# v0.6.25 Release Candidates

Status: **released**. `v0.6.25` is the current public GitHub release. `v0.6.24`
is the historical previous public release. `v0.6.26` is the next planned release
line. The `v0.6.25` tag and GitHub Release exist; PyPI was not published.

- Status: released
- Current public release: `v0.6.25`
- Previous public release: `v0.6.24`
- Next planned release: `v0.6.26`
- Release date: 2026-07-13
- PyPI: not published
- Tag created: yes
- GitHub Release: created

## Candidate acceptance

Two candidates, **CAND-019 — Docs/config hardcoded release-literal sweep** and
**CAND-020 — Post-v0.6.24 test drift sweep**, are **released** as part of
`v0.6.25` on 2026-07-13. Release is documentation/governance/test-only and does
not authorize live trading, live submit, order placement, broker/provider
execution, credential loading, network access, or approval queue mutation.

## Released

- **CAND-019 — Docs/config hardcoded release-literal sweep** — released on
  2026-07-13 as part of `v0.6.25`.
  - Extends `scripts/check_hardcoded_release_literals.py` to scan `configs/`,
    `examples/`, and `docs/examples/` for monitored release-identity literals.
  - Adds regression tests to keep the new scan targets synchronized with
    `release-metadata.json`.
  - Static checker/test-only; no runtime, safety, broker, provider, credential,
    network, or trading changes.

- **CAND-020 — Post-v0.6.24 test drift sweep** — released on 2026-07-13 as part
  of `v0.6.25`.
  - Updates stale post-`v0.6.24` test fixtures and assertions to the current
    release identity (`0.6.25` / `v0.6.25` / `v0.6.26`).
  - Adds `0.6.24` to the `SECURITY.md` supported-versions table.
  - Records this candidate in the v0.6.25 candidate chain.
  - Test-only drift fixes; no runtime, safety, broker, provider, credential,
    network, or trading changes.

## Proposed

No additional candidates are currently proposed for `v0.6.26`. See
[v0.6.26-plan.md](v0.6.26-plan.md) for possible future directions, none of which
are implemented or accepted.

## Safety boundaries

No `v0.6.25` content enables live trading, live submit, broker/provider
execution, credential loading, network access, order placement, pending-order
creation, or approval queue mutation, and none changes `RiskManager`,
kill-switch, deadman, heartbeat, or audit hash-chain behavior. The CAND-014
provider-artifact extraction boundary remains strictly at
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
`atlas run --mode live` remains fail-closed. PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.25` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.
