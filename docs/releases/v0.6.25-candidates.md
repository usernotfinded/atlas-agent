# v0.6.25 Release Candidates

Status: **planning**. `v0.6.25` is the next planned release line and is not
released. `v0.6.24` is the current public GitHub release. `v0.6.23` is the
historical previous public release. No `v0.6.25` tag, GitHub Release, or PyPI
publication exists.

- Status: planning
- Current public release: `v0.6.24`
- Previous public release: `v0.6.23`
- Next planned release: `v0.6.25`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

## Candidate acceptance

No candidates are accepted into the `v0.6.25` candidate chain yet. Opening this
planning line is documentation/governance only and does not authorize live
trading, live submit, order placement, broker/provider execution, credential
loading, network access, or approval queue mutation.

## Accepted

No candidates are currently accepted for `v0.6.25`.

## Proposed

No candidates are currently proposed for `v0.6.25`. See
[v0.6.25-plan.md](v0.6.25-plan.md) for possible future directions, none of which
are implemented or accepted.

## Safety boundaries

No `v0.6.25` planning content enables live trading, live submit, broker/provider
execution, credential loading, network access, order placement, pending-order
creation, or approval queue mutation, and none changes `RiskManager`,
kill-switch, deadman, heartbeat, or audit hash-chain behavior. The CAND-014
provider-artifact extraction boundary remains strictly at
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
`atlas run --mode live` remains fail-closed. PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.25` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.
