# v0.6.5 Planning

> **Not financial advice.** Atlas Agent is a software tool, not a financial advisor. Trading involves significant risk of loss.

## Status

This document is a planning-only artifact created after the `v0.6.4` release.
`v0.6.4` is the current public GitHub release (tagged and published). `v0.6.5` is
not version-prepared, not tagged, and not released. This plan does not authorize
implementation, version bumps, tag creation, GitHub release creation, or PyPI
publishing.

## Context

The `v0.6.4` release cutover completed on 2026-06-07. Commit `f3972ef`
(`fix: validate security docs with package version`) was pushed after the
release to fix a false negative in `scripts/release_assurance.py` where
`SECURITY.md` validation incorrectly compared the tag-formatted version
(`v0.6.4`) against the package version (`0.6.4`). The fix now validates against
the package version correctly.

The `v0.6.5` release line is reserved for small, backward-compatible
maintenance improvements to docs, checkers, and public-release reference
consistency. No new runtime features, broker adapters, provider integrations, or
trading behavior changes are planned in this document.

## Candidate Categories

Candidates for `v0.6.5` are limited to the following categories:

- Post-release docs consistency and stale-reference cleanup
- Release-assurance/checker robustness follow-up
- Package/tag version normalization across checkers
- Docs/checker drift prevention
- CI deterministic reliability
- Public docs consistency expansion

## Candidate Table

| ID | Candidate | Category | Status | Rationale |
|---|---|---|---|---|
| CAND-001 | Update trust center and public docs to reflect `v0.6.4` as current public release | docs consistency | proposed | `docs/trust/README.md`, `docs/security/release-readiness.md`, and `docs/public-launch-readiness.md` still reference `v0.6.3` as current public release or describe `v0.6.4` as not yet tagged. |
| CAND-002 | Normalize package version vs tag version handling in historical checkers | checker robustness | proposed | Several historical checkers (`check_v058_rc1_readiness.py`, `check_v061_release_prep.py`, etc.) use tag-formatted versions in some assertions and package versions in others. A consistency pass reduces future false negatives. |
| CAND-003 | Add regression test for `security_md_current` package-version validation | test coverage | proposed | The `f3972ef` fix should have test coverage to prevent regression if `release_assurance.py` is refactored. |
| CAND-004 | Update release checklist examples to reference current source/public versions | docs consistency | proposed | `docs/release-checklist.md` contains historical example commands and version references that can drift after each release. |
| CAND-005 | Harden deterministic ordering in additional evidence/report JSON outputs | CI determinism | proposed | Some report generators may still use `list(set(...))` or dict iteration without sorting, causing flaky diffs across platforms. |
| CAND-006 | Add `v0.6.5` release prep checker skeleton | checker infrastructure | proposed | Following the established pattern, prepare `scripts/check_v065_release_prep.py` and `tests/test_v065_release_prep.py` in planning mode so they activate after the v0.6.5 version bump. |

## Accepted Candidates

| ID | Candidate | Category | Reason |
|---|---|---|---|
| CAND-003 | Regression test for `security_md_current` package-version validation | test coverage | Small, test-only regression guard for an important fix. No runtime changes. |
| CAND-004 | Update release checklist version references | docs consistency | Low-cost docs hygiene to remove stale historical version examples. No runtime changes. |
| CAND-006 | `v0.6.5` release prep checker skeleton | checker infrastructure | Follows established pattern; prepares the release-prep gate without enabling it prematurely. No runtime changes. |

Acceptance requires:
- owner approval,
- passing `dev_check`, `ci_check`, and `release_check --quick`,
- no protected boundary changes,
- no new runtime trading/broker/provider/approval/kill-switch behavior.

## Deferred Candidates

| ID | Candidate | Reason |
|---|---|---|
| CAND-005 | Harden deterministic ordering in JSON outputs | Most JSON outputs already use `sort_keys=True`; remaining cases are in complex research modules where a dedicated determinism pass is more appropriate than a patch-scope change. |
| DEF-001 | New broker adapters (Binance completion, CCXT enablement, IBKR implementation) | New features, not bug fixes. Defer to v0.7.0 or later minor release. |
| DEF-002 | New strategy packs or backtest engine expansion | New features out of patch scope. Defer to v0.7.0 or later. |
| DEF-003 | Dashboard rewrite or new UI capabilities | Out of patch scope. Requires design review. |
| DEF-004 | Major architecture refactor | Out of patch scope. Too risky for a patch release. |
| DEF-005 | Research-to-live trading bridge | Safety-sensitive. Requires explicit gated design. |

## Rejected / Out-of-Scope Items

The following items are explicitly rejected for `v0.6.5`:

- **Live trading default changes** — Atlas remains disabled-by-default for live trading.
- **Broker execution enablement** — No broker adapter may be promoted to default live-submit enabled.
- **Provider execution unlock** — Provider adapters remain fail-closed and disabled by default.
- **Autonomous trading** — Not claimed or enabled.
- **PyPI publish** — Not approved in this planning batch; requires separate process.
- **Major dashboard rewrite** — Out of patch scope.
- **Large architecture rewrite** — Out of patch scope and too risky.
- **New broker adapters** — New features, not maintenance.
- **New strategy packs** — New features, not maintenance.
- **Automatic skill activation** — Skill candidates remain manually reviewed and offline.
- **Automatic learning execution** — Learning suggestions remain offline and manually reviewed.
- **Profitability or trading correctness claims** — Forbidden.
- **Financial advice wording** — Forbidden.

## Safety Boundaries

Regardless of which candidates are accepted later, these boundaries remain
non-negotiable for any `v0.6.5` work:

- Live trading remains disabled by default.
- Live submit remains disabled by default.
- Provider execution remains disabled by default.
- Broker execution remains disabled by default.
- Risk gates remain deterministic and unchanged.
- Approval queue behavior remains unchanged.
- Kill-switch behavior remains unchanged.
- Human approval remains required for live order flow.
- Credentials are not loaded unless explicitly configured.
- No provider APIs, broker APIs, or real webhooks are called from CI or local checks.

## Release Criteria

Before `v0.6.5` can be prepared or tagged, the following must be true:

- [ ] Accepted candidates are implemented.
- [ ] `dev_check`, `ci_check`, and `release_check --quick` pass locally.
- [ ] CI on `main` is green after the prep commit is pushed.
- [ ] Version is bumped to `0.6.5` in `pyproject.toml` and `src/atlas_agent/__init__.py`.
- [ ] `docs/releases/v0.6.5.md` and `docs/trust/v0.6.5-status.md` are created.
- [ ] `CHANGELOG.md` has a `[0.6.5]` entry.
- [ ] `scripts/check_v065_release_prep.py` and matching tests are created.
- [ ] Protected boundary diff is empty.
- [ ] Forbidden claims scan is clean.
- [ ] Owner approval is obtained for tag cutover.
- [ ] No secrets or API-key-like strings are added.

## Non-Goals

- This plan does not bump the version.
- This plan does not create `docs/releases/v0.6.5.md`.
- This plan does not create a tag or GitHub release.
- This plan does not publish to PyPI.
- This plan does not change runtime trading behavior.

## Next Steps

1. Review the detailed candidate selection in
   [`docs/releases/v0.6.5-candidates.md`](v0.6.5-candidates.md) and its
   machine-readable inventory [`v0.6.5-candidates.json`](v0.6.5-candidates.json).
2. Accept or reject candidates based on the selection criteria and safety
   boundaries documented there.
3. Implement accepted candidates in small, reviewable batches.
4. Run required local checks after each batch.
5. When all accepted candidates are complete, bump version to `0.6.5` and create
   release prep artifacts (`docs/releases/v0.6.5.md`,
   `docs/trust/v0.6.5-status.md`, `scripts/check_v065_release_prep.py`,
   `tests/test_v065_release_prep.py`).
6. Run full release prep verification.
7. Obtain owner approval before tag creation.
8. Create annotated tag `v0.6.5` and GitHub release only after approval.

---

*This planning document was created on 2026-06-07 as part of post-v0.6.4 release
hygiene. It does not authorize any tag, release, or publish action.*
