# v0.6.20 Release Candidates

Status: **released** as `v0.6.20` on 2026-07-02. `v0.6.20` is the current public
GitHub release. `v0.6.19` is the historical previous public release. `v0.6.21`
is the next planned release line. No `v0.6.21` tag, GitHub Release, or PyPI
publication exists.

- Status: released
- Current public release: `v0.6.20`
- Previous public release: `v0.6.19`
- Next planned release: `v0.6.21`
- PyPI: not published
- Tag created: true
- GitHub Release: created

## Candidate acceptance

**CAND-012** is accepted into the `v0.6.20` candidate chain with acceptance
verdict `PASS_WITH_WARNINGS` on 2026-07-01 and is released in `v0.6.20`. This
acceptance does not authorize live trading, live submit, order placement,
broker/provider execution, credential loading, network access, or approval queue
mutation.

## Implemented

- **CAND-012:** Candidate-Chain Consistency Guard.
  - Adds `scripts/check_candidate_chain.py`, a deterministic local read-only
    checker that validates release-metadata / candidate-chain JSON / candidate-chain
    Markdown consistency.
  - Adds `tests/test_candidate_chain.py` with fixture-based coverage using
    `tmp_path` and subprocess invocation.
  - Integrates the checker into `scripts/dev_check.sh` and `scripts/ci_check.sh`
    after release metadata and version consistency checks.
  - Documents the checker in `docs/development/checks-reference.md`.
  - Validates candidate IDs, statuses, acceptance verdicts, release lines,
    tag/GitHub Release/PyPI claims, and candidate-chain-specific forbidden claims.
  - Uses exit codes `0` (pass), `1` (operational error), and `2` (validation
    failure) and prints `Candidate-chain consistency PASSED` on success.
  - Uses only the Python standard library, performs no network access, loads no
    credentials, imports no `atlas_agent` runtime code, and does not mutate
    repository files.
  - Design doc: `docs/candidate-chain-consistency-guard-design.md`
  - Implementation plan: `docs/candidate-chain-consistency-guard-implementation-plan.md`
  - Design commit: `027cc36eda401c9714630beaf29a2dc9b66c11ef`
  - Implementation-plan commit: `484690ca88d55e2ef70da9ed90545122d088a199`
  - Implementation commit: `bce521efc1ddbb88ea5ca308d2b17ad867c6c536`
  - Acceptance commit: this document is updated by the CAND-012 acceptance commit.
  - Design review verdict: `PASS_WITH_WARNINGS`
  - Implementation-plan review verdict: `PASS_WITH_WARNINGS`
  - Implementation review verdict: `PASS_WITH_WARNINGS`

## Proposed

No additional candidates beyond CAND-012 are currently proposed for `v0.6.20`.

## Known warnings (non-blocking)

1. A full `pytest` run or full `bash scripts/release_check.sh` may exceed local
   interactive timeouts. The `--quick` gate and all targeted checks pass.
2. Historical candidate-chain docs for pre-`v0.6.20` releases still emit
   expected warnings from `check_candidate_chain.py` because they reflect
   historical planning state rather than current metadata.
