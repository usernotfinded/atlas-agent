# v0.6.21 Release Plan

Status: **planning**. `v0.6.21` is the next planned release line and is not
released.

- Status: planning
- Current public release: `v0.6.20`
- Previous public release: `v0.6.19`
- Next planned release: `v0.6.21`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

`v0.6.20` is the current public GitHub release. `v0.6.19` is the historical
previous public release. The source/package version is `0.6.20`. `v0.6.21` is
the next planned release line; no `v0.6.21` tag, GitHub Release, or PyPI
publication exists, and the v0.6.21 release cutover has not started.

## Candidates under consideration

| id | title | status | safety notes |
|---|---|---|---|
| CAND-013 | Public/Trust Docs Drift Coverage Guard | accepted into v0.6.21 (planning-only) | Extends the public-docs consistency checker to catch stale trust README current-public labels, autonomy-roadmap candidate-state contradictions against next-planned candidate-chain JSON, and related drift, using release metadata as authority; stdlib-only, no network, no credentials, no runtime imports; preserves exit codes `0`/`1`. No live trading, live submit, broker/provider execution, credential loading, or order placement. No safety-module changes. |
| CAND-014 | Provider Artifact Engine Deduplication - Phase 2 pilot extraction only | accepted into v0.6.21 (planning-only; PASS) | Phase 2 one-module pilot extraction moves shared artifact mechanics for `src/atlas_agent/research/provider_mock_response_final_safety_seal.py` behind a minimal `src/atlas_agent/research/artifact_engine.py` while preserving Phase 1 golden compatibility (`13 passed`) and existing final-safety-seal/upstream mock-response chain tests (`299 passed`). Heavy gates passed during independent review. Extraction beyond the pilot remains not authorized. No provider, CLI, checker, runtime, safety, version, release-metadata, tag, GitHub Release, or PyPI changes. |
| CAND-015 | Bounded Live Autonomy Readiness Gate | accepted into v0.6.21 (planning-only; PASS) | Adds `src/atlas_agent/agent/bounded_live_autonomy_readiness.py` engine and `src/atlas_agent/agent/bounded_live_autonomy_readiness_cli.py` configless CLI, registers `atlas agent bounded-live-readiness`, adds static contract checker, tests, and fixtures. Consumes upstream CAND-004/CAND-005/CAND-006/CAND-007/CAND-008 artifacts plus CAND-015-owned policy fixtures; evaluates a 15-gate fail-closed sequence; records `bounded-live-readiness.json` and `bounded-live-readiness-report.md`. Does not submit orders, call brokers/providers, load credentials, mutate runtime state, or claim live readiness. No runtime, safety, version, release-metadata, tag, GitHub Release, or PyPI changes. |

## Acceptance

CAND-013 is accepted into the `v0.6.21` candidate chain with acceptance verdict
`PASS_WITH_WARNINGS` on 2026-07-04 and its independent implementation-review
verdict is `PASS_WITH_WARNINGS`. The acceptance is planning-only and does not
authorize live trading, live submit, order placement, broker/provider execution,
credential loading, network access, or approval queue mutation. The `v0.6.21`
release cutover is a separate future step and has not started.

CAND-014 Phase 2 is accepted into the `v0.6.21` candidate chain with acceptance
verdict `PASS` on 2026-07-06 and its independent Phase 2
implementation-review verdict is `PASS`. Phase 1 remains accepted with verdict
`PASS_WITH_WARNINGS`. The accepted Phase 2 scope is a one-module pilot extraction
that moves shared artifact mechanics for
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py` behind a
minimal `src/atlas_agent/research/artifact_engine.py` while preserving Phase 1
golden compatibility. Design status is complete, implementation-plan status is
complete, Phase 1 implementation status is complete, and Phase 2 pilot
implementation status is complete. Extraction beyond this single pilot module is
not authorized. The `v0.6.21` release cutover is a separate future step and has
not started.

CAND-015 is accepted into the `v0.6.21` candidate chain with acceptance verdict
`PASS` on 2026-07-06. Its implementation-review verdict is `PASS`. The accepted
scope is an evidence-only, simulated-only L2/L3 autonomy readiness gate:
`src/atlas_agent/agent/bounded_live_autonomy_readiness.py`,
`src/atlas_agent/agent/bounded_live_autonomy_readiness_cli.py`, the registered
`atlas agent bounded-live-readiness` command,
`scripts/check_bounded_live_autonomy_readiness_contract.py`, and associated
tests and fixtures. It consumes upstream CAND-004/CAND-005/CAND-006/CAND-007/
CAND-008 artifacts plus CAND-015-owned local policy fixtures, evaluates a strict
15-gate fail-closed sequence, and records `bounded-live-readiness.json` and
`bounded-live-readiness-report.md` only when all gates pass. It does not submit
orders, call brokers/providers, load credentials, mutate runtime state, or claim
live readiness. `bounded_live_readiness_recorded` is evidence-recording status
only and not permission to trade or authorization to submit orders. Design status
is complete and implementation status is complete. The `v0.6.21` release cutover
is a separate future step and has not started.

## Known warnings (non-blocking)

1. False-positive-mitigation asymmetry: the inline current-public block in
   `_check_stale_release_status_lines` does not honor `historical_context` while
   an adjacent labeled/inline public-claim block does. No impact on the current
   repository; a future hardening candidate. Non-blocking.
2. Methodological live-mode verification note: `atlas run --mode live` was
   verified via a safe isolated paper/null-provider workspace rather than against
   the local live config with credentials. This preserved review boundaries and
   is acceptable because CAND-013 changed no runtime, safety, CLI, broker,
   provider, or credential-loading code. Non-blocking.
3. Historical candidate-chain docs for pre-`v0.6.21` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata.
4. CAND-014 inventory-count warning: the Phase 1 inventory doc records that the
   broad provider-related test-file command returned 87 files, while a later
   live filesystem command returned 120 because it included ignored
   `__pycache__` entries plus current Phase 1 files. The Phase 1 source
   inventory remains correct: 22 `provider_*.py` modules and 28,908 source
   lines. Non-blocking.
5. CAND-014 strict pilot boundary: extraction beyond
   `provider_mock_response_final_safety_seal.py` remains not authorized. Any
   further extraction requires a separate planning/review step. Non-blocking.
6. CAND-014 ID classification: PASS with a non-blocking legacy reuse warning.
   `CAND-014` is the correct current-epoch next ID for `v0.6.21` after
   CAND-013. The older `v0.6.12` `CAND-014` belongs to the legacy release-local
   epoch, and the candidate-chain checker enforces uniqueness within each JSON
   file rather than globally.
7. CAND-015 status semantics: `bounded_live_readiness_recorded` is
   evidence-recording status only. It is not live readiness, not trading safety,
   not profitability evidence, not permission to trade, and not authorization to
   submit orders. L3 bounded live autonomy remains a future research concept.
   Non-blocking.
8. CAND-015 live-mode verification note: `atlas run --mode live` is verified via
   a safe isolated paper/null-provider workspace rather than against a local live
   config with credentials, because CAND-015 changes no runtime, safety, CLI,
   broker, provider, or credential-loading code. Non-blocking.
