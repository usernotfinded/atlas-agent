# v0.6.22 Release Candidates

Status: **planning**. `v0.6.22` is the next planned release line and is not
released. `v0.6.21` is the current public GitHub release. `v0.6.20` is the
historical previous public release. No `v0.6.22` tag, GitHub Release, or PyPI
publication exists.

- Status: planning
- Current public release: `v0.6.21`
- Previous public release: `v0.6.20`
- Next planned release: `v0.6.22`
- PyPI: not published
- Tag created: no
- GitHub Release: not created

## Candidate acceptance

**CAND-016** is **accepted** into the `v0.6.22` candidate chain. Acceptance is
documentation/governance only and does not authorize live trading, live submit,
order placement, broker/provider execution, credential loading, network access,
or approval queue mutation. The `v0.6.22` release line remains in planning status
and is not released.

## Accepted

- **CAND-016: Release-Maintenance Drift Hardening** — accepted on 2026-07-13 with
  verdict **PASS** (not released).
  - Hardens the release-maintenance path so release-identity state stays
    metadata-driven instead of drifting behind hardcoded version literals.
  - Adds a reverse-drift roadmap guard to
    `scripts/check_public_docs_consistency.py`
    (`_released_candidate_ids` and
    `_check_autonomy_roadmap_released_candidates_not_next_planned`) that flags an
    already-released candidate listed as a candidate-entry bullet under the
    next-planned planning line, plus deterministic tests; retains the
    metadata-driven release-assurance demo default.
  - Docs/checker/test-only: no runtime, safety, CLI, broker, provider, credential,
    version, or release-metadata behavior changes; does not broaden the CAND-014
    boundary.
  - Directly addresses the drift observed after the `v0.6.21` cutover (stale
    next-planned tag guard, stale demo default, roadmap candidate mislabeling).
  - Design and plan: [docs/cand-016-release-maintenance-drift-hardening.md](../cand-016-release-maintenance-drift-hardening.md)
  - The supporting guard and tests have landed on `main` as unreleased work.

## Proposed

No candidates are currently proposed for `v0.6.22`.

## Safety boundaries

No `v0.6.22` planning content enables live trading, live submit, broker/provider
execution, credential loading, network access, order placement, pending-order
creation, or approval queue mutation, and none changes `RiskManager`,
kill-switch, deadman, heartbeat, or audit hash-chain behavior. The CAND-014
provider-artifact extraction boundary remains strictly at
`src/atlas_agent/research/provider_mock_response_final_safety_seal.py`.
`atlas run --mode live` remains fail-closed. PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.22` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.
