# v0.6.25 Release Plan

Status: **released**. `v0.6.25` is the current public GitHub release. `v0.6.24`
is the historical previous public release. `v0.6.26` is the next planned release
line.

- Status: released
- Current public release: `v0.6.25`
- Previous public release: `v0.6.24`
- Next planned release: `v0.6.26`
- Release date: 2026-07-13
- PyPI: not published
- Tag created: yes
- GitHub Release: created

`v0.6.25` is the current public GitHub release. `v0.6.24` is the historical
previous public release. The source/package version is `0.6.25`. The `v0.6.25`
tag and GitHub Release exist; PyPI was not published. The `v0.6.25` release
cutover has completed.

The `v0.6.25` release state, CI status, and post-release checks will be recorded
in [v0.6.25 Post-Release Assurance](v0.6.25-post-release-assurance.md) once
available.

## Candidates under consideration

Two candidates are released in `v0.6.25`.

| id | title | status | safety notes |
|---|---|---|---|
| CAND-019 | Docs/config hardcoded release-literal sweep | released (PASS, 2026-07-13) | Static checker/test-only. Extends release-literal scanning to `configs/`, `examples/`, and `docs/examples/`. Adds regression tests. Stdlib only; no network, credentials, or file mutation. No runtime/safety/broker/provider/credential changes; CAND-014 boundary unchanged. |
| CAND-020 | Post-v0.6.24 test drift sweep | released (PASS, 2026-07-13) | Test-only drift fixes. Updates stale post-v0.6.24 fixtures to current release identity, adds `0.6.24` to `SECURITY.md`, records candidate. No runtime/safety/broker/provider/credential changes. |

## Possible future candidates

These directions were listed during the `v0.6.25` planning pass. They were not
implemented, designed, or accepted, and listing them here authorizes no runtime,
safety, broker, provider, credential, network, or trading behavior. Each would
require its own design, implementation plan, review, and acceptance before
landing in a future release line.

- Regression coverage for the hardcoded release-literal checker (e.g.,
  additional context shapes, confidence scoring, or historical-checker pattern
  validation) while keeping the scan bounded to active scripts and metadata-
  driven literals.
- Continued provider-artifact deduplication planning, keeping the CAND-014
  extraction boundary strictly at
  `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`; no
  broadening is authorized.

## Post-release hardening landed in this release

- Extended `scripts/check_hardcoded_release_literals.py` and
  `tests/test_check_hardcoded_release_literals.py` to also scan `configs/`,
  `examples/`, and `docs/examples/` for hardcoded release-identity literals
  before cutover. The checker uses the Python standard library only, performs no
  network access, loads no credentials, and does not mutate repository files.
  Test-only; no runtime, safety, broker, provider, credential, or CLI behavior
  changed.
- Refreshed post-`v0.6.24` test fixtures and assertions to the current release
  identity and added `0.6.24` to the `SECURITY.md` supported-versions table.

## Safety boundaries

This release enables nothing at runtime. Specifically, no candidate or document
in `v0.6.25`:

- enables live trading or makes live trading a default;
- enables live order submission or changes `can_submit`;
- enables broker or provider execution, or calls real brokers or providers;
- loads broker/provider credentials;
- adds network access;
- places, cancels, or flattens orders, or creates pending orders;
- mutates approval queues, the kill switch, heartbeat, deadman, or the audit
  hash-chain;
- broadens the CAND-014 provider-artifact extraction boundary.

`atlas run --mode live` remains fail-closed. This is a GitHub-only project and
PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.25` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

## Not financial advice

Atlas Agent is a software tool, not a financial advisor. Trading involves
significant risk of loss. This plan makes no profitability, trading-safety,
live-readiness, production-readiness, or unattended-safe-operation claim.
