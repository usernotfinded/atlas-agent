# v0.6.21 Candidate Selection Gate

> **Status:** planning (`v0.6.21` next planned release line; not released)
> **Current public release:** `v0.6.20`
> **Previous public release:** `v0.6.19`
> **Next planned release:** `v0.6.21`
> **PyPI:** not published
> **Tag created:** no
> **GitHub Release:** not created
> **Not financial advice.** Atlas Agent is a software tool, not a financial
> advisor. Trading involves significant risk of loss.

`v0.6.20` is the current public GitHub release. `v0.6.19` is the historical
previous public release. CAND-013 is accepted into the `v0.6.21` candidate chain
as a docs/checker/test-only guard. This acceptance is bounded/safety-only: it
does not authorize live trading, live submit, order placement, broker execution,
provider execution, credential loading, network access, or approval queue
mutation.

Current public release: `v0.6.20`. Source/package version: `0.6.20`. PyPI is not
published. `v0.6.21` is planning-only; no `v0.6.21` tag, GitHub Release, or PyPI
publication exists.

## Proposed candidates

- **CAND-013:** Public/Trust Docs Drift Coverage Guard.

### Why CAND-013 was eligible

- It is a docs/checker/test-only addition; it does not implement, authorize, or
  enable any trading or execution path.
- It strengthens `scripts/check_public_docs_consistency.py` so stale trust README
  current-public labels, autonomy-roadmap candidate-state contradictions against
  next-planned candidate-chain JSON, and related public-docs drift are caught
  before future release cutovers, using release metadata as the authority.
- It adds `tests/test_public_docs_consistency.py` coverage (`52 passed`), updates
  `docs/development/checks-reference.md`, and corrects one autonomy-roadmap
  current/historical/planning line.
- It uses only the Python standard library, performs no network access, loads no
  credentials, imports no `atlas_agent` runtime code, and does not mutate
  repository files.
- It introduces no live trading, live submit, broker/provider execution,
  credential loading, order placement, pending-order creation, network access,
  or approval queue mutation.
- It changes no `RiskManager`, kill-switch, deadman, heartbeat, or audit
  hash-chain code, and no version or release-metadata files.
- It preserves the checker's existing exit codes (`0` pass, `1`
  violations/errors) and adds no new exit code.
- It includes deterministic tests and passes the targeted verification commands.

## Candidate acceptance

**CAND-013** is accepted into the `v0.6.21` candidate chain with acceptance
verdict `PASS_WITH_WARNINGS` on 2026-07-04. This acceptance does not change
runtime behavior beyond the checker/test/docs already landed on `main`, nor does
it enable live trading or live submit, authorize order placement, load
credentials, call brokers or providers, access the network, or mutate runtime
state or approval queues. It is not an approval to trade, not a live-readiness
claim, and not permission to submit orders. `v0.6.21` remains planning-only and
is not released.

### Selection rules

CAND-013 satisfies the repository's candidate selection rules:

1. No live trading enablement — fail-closed defaults unchanged.
2. No broker/provider execution enablement — no direct AI-to-broker path.
3. No secrets or credentials committed.
4. No unsafe marketing claims — no promises of specific returns, eliminated
   risk, live readiness, or autonomous trading readiness.
5. Protected runtime boundaries preserved — changes are confined to a static
   checker, its tests, and documentation.
6. Small candidate batch — reviewable and independently valuable.
7. Test/doc coverage — deterministic tests and updated candidate-chain
   documentation.
8. Green verification before acceptance — passes targeted verification commands.

## Known warnings (non-blocking)

1. False-positive-mitigation asymmetry: the inline current-public block in
   `_check_stale_release_status_lines` does not honor `historical_context` while
   an adjacent labeled/inline public-claim block does. No impact on the current
   repository; a future hardening candidate. Non-blocking.
2. Methodological live-mode verification note: `atlas run --mode live` was
   verified via a safe isolated paper/null-provider workspace rather than against
   the local live config with credentials. This preserved review boundaries and
   is acceptable because CAND-013 changed no runtime, safety, CLI, broker,
   provider, or credential-loading code. Non-blocking.
3. Historical candidate-chain docs for pre-`v0.6.21` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata.

This candidate selection is documentation/governance acceptance only. The
`v0.6.21` release cutover is a separate future step and has not started.
