# v0.6.4 Planning

> **Not financial advice.** Atlas Agent is a software tool, not a financial advisor. Trading involves significant risk of loss.

## Status

This document is a planning-only artifact created after the `v0.6.3` release.
`v0.6.3` is the current public GitHub release (tagged). `v0.6.4` is not yet
prepared, not tagged, and not released. This plan does not authorize
implementation, version bumps, tag creation, GitHub release creation, or PyPI
publishing.

## Scope

The `v0.6.4` release line is reserved for small, backward-compatible
maintenance improvements to docs, checkers, CI reliability, and local
evidence-handling hygiene. No new runtime features, broker adapters, provider
integrations, or trading behavior changes are planned in this document.

## Candidate Categories

Candidates for `v0.6.4` are limited to the following categories:

- Post-release docs polish and stale-reference cleanup
- Release-assurance/checker reliability improvements
- Package distribution checker follow-up hardening
- CI determinism and long-running check observability
- Generated artifact hygiene and local evidence handling
- Documentation consistency and trust-center maintenance
- Test isolation for environment-sensitive checks

## Candidate Table

| ID | Candidate | Category | Status | Rationale |
|---|---|---|---|---|
| CAND-001 | Refresh public docs after each release to remove stale "version-prepared" wording | docs polish | proposed | Prevents drift between released tags and public messaging. |
| CAND-002 | Harden `release_assurance.py` pre-tag/post-tag state reporting | checker reliability | proposed | Reduce confusion when the script is run before vs. after a tag exists. |
| CAND-003 | Add timeout and progress diagnostics to long-running local gates | CI observability | proposed | Improves local and CI debugging when gates run near timeout limits. |
| CAND-004 | Improve generated artifact backup/cleanup guidance | artifact hygiene | proposed | Keeps local evidence folders predictable without deleting historical CI artifacts. |
| CAND-005 | Add deterministic ordering coverage for additional evidence JSON outputs | CI determinism | proposed | Reduces flaky diff comparisons across macOS/Linux CI runners. |
| CAND-006 | Update `checks-reference.md` after each new release prep checker | docs consistency | proposed | Keeps checker examples and version references current. |

## Accepted Candidates

None accepted yet. Acceptance requires:
- owner approval,
- passing `dev_check`, `ci_check`, and `release_check --quick`,
- no protected boundary changes,
- no new runtime trading/broker/provider/approval/kill-switch behavior.

## Deferred Candidates

| ID | Candidate | Reason |
|---|---|---|
| DEF-001 | New broker adapters (Binance completion, CCXT enablement, IBKR implementation) | New features, not bug fixes. Defer to v0.7.0 or later minor release. |
| DEF-002 | New strategy packs or backtest engine expansion | New features out of patch scope. Defer to v0.7.0 or later. |
| DEF-003 | Dashboard rewrite or new UI capabilities | Out of patch scope. Requires design review. |
| DEF-004 | Major architecture refactor | Out of patch scope. Too risky for a patch release. |
| DEF-005 | Research-to-live trading bridge | Safety-sensitive. Requires explicit gated design. |

## Rejected / Out-of-Scope Items

The following items are explicitly rejected for `v0.6.4`:

- **Live trading default changes** — Atlas remains disabled-by-default for live trading.
- **Broker execution enablement** — No broker adapter may be promoted to default live-submit enabled.
- **Provider execution unlock** — Provider adapters remain fail-closed and disabled by default.
- **Autonomous trading** — Not claimed or enabled.
- **PyPI publish** — Not approved in this planning batch; requires separate process.
- **Major dashboard rewrite** — Out of patch scope.
- **Large architecture rewrite** — Out of patch scope and too risky.
- **New broker adapters** — New features, not maintenance.
- **New strategy packs** — New features, not maintenance.
- **Profitability or trading correctness claims** — Forbidden.
- **Financial advice wording** — Forbidden.

## Safety Boundaries

Regardless of which candidates are accepted later, these boundaries remain
non-negotiable for any `v0.6.4` work:

- Live trading remains disabled by default.
- Live submit remains disabled by default.
- Provider execution remains disabled by default.
- Broker execution remains disabled by default.
- Risk gates remain deterministic and unchanged.
- Approval queue behavior remains unchanged.
- Kill-switch behavior remains unchanged.
- Human approval remains required for live order flow.
- Credentials are not loaded unless explicitly configured.
- No provider APIs, broker APIs, or real webhooks are called from CI or local checks.

## Release Criteria

Before `v0.6.4` can be prepared or tagged, the following must be true:

- [ ] Accepted candidates are implemented.
- [ ] `dev_check`, `ci_check`, and `release_check --quick` pass locally.
- [ ] CI on `main` is green after the prep commit is pushed.
- [ ] Version is bumped to `0.6.4` in `pyproject.toml` and `src/atlas_agent/__init__.py`.
- [ ] `docs/releases/v0.6.4.md` and `docs/trust/v0.6.4-status.md` are created.
- [ ] `CHANGELOG.md` has a `[0.6.4]` entry.
- [ ] `scripts/check_v064_release_prep.py` and matching tests are created.
- [ ] Protected boundary diff is empty.
- [ ] Forbidden claims scan is clean.
- [ ] Owner approval is obtained for tag cutover.
- [ ] No secrets or API-key-like strings are added.

## Non-Goals

- This plan does not bump the version.
- This plan does not create `docs/releases/v0.6.4.md`.
- This plan does not create a tag or GitHub release.
- This plan does not publish to PyPI.
- This plan does not change runtime trading behavior.

## Next Steps

1. Review the detailed candidate selection in
   [`docs/releases/v0.6.4-candidates.md`](v0.6.4-candidates.md) and its
   machine-readable inventory [`v0.6.4-candidates.json`](v0.6.4-candidates.json).
2. Accept or reject candidates based on the selection criteria and safety
   boundaries documented there.
3. Implement accepted candidates in small, reviewable batches.
4. Run required local checks after each batch.
5. When all accepted candidates are complete, bump version to `0.6.4` and create
   release prep artifacts (`docs/releases/v0.6.4.md`,
   `docs/trust/v0.6.4-status.md`, `scripts/check_v064_release_prep.py`,
   `tests/test_v064_release_prep.py`).
6. Run full release prep verification.
7. Obtain owner approval before tag creation.
8. Create annotated tag `v0.6.4` and GitHub release only after approval.

---

*This planning document was created on 2026-06-06 as part of post-v0.6.3 release hygiene. It does not authorize any tag, release, or publish action.*
