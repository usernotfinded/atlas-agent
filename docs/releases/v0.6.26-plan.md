# v0.6.26 Release Plan

Status: **released**. `v0.6.26` is the current public GitHub release. `v0.6.25`
is the historical previous public release. `v0.6.27` is the next planned release
line.

- Status: released
- Current public release: `v0.6.26`
- Previous public release: `v0.6.25`
- Next planned release: `v0.6.27`
- Release date: 2026-07-13
- PyPI: not published
- Tag created: yes
- GitHub Release: created

`v0.6.26` is the current public GitHub release. `v0.6.25` is the historical
previous public release. The source/package version is `0.6.26`. The `v0.6.26`
tag and GitHub Release exist; PyPI was not published. The `v0.6.26` release
cutover has completed.

The `v0.6.26` release state, CI status, and post-release checks will be recorded
in [v0.6.26 Post-Release Assurance](v0.6.26-post-release-assurance.md) once
available.

## Candidates under consideration

One candidate is released in `v0.6.26`.

| id | title | status | safety notes |
|---|---|---|---|
| CAND-021 | Metadata-driven current-state test fixtures | released (PASS, 2026-07-13) | Test/docs-only. Adds shared `release_identity` pytest fixture from `release-metadata.json` and refactors current-state tests to use fixture values. No runtime/safety/broker/provider/credential changes; CAND-014 boundary unchanged. |

## Possible future candidates

These directions were listed during the `v0.6.26` planning pass. They were not
implemented, designed, or accepted, and listing them here authorizes no runtime,
safety, broker, provider, credential, network, or trading behavior. Each would
require its own design, implementation plan, review, and acceptance before
landing in a future release line.

- Continued hardcoded release-literal checker coverage while keeping the scan
  bounded to active scripts and metadata-driven literals.
- Continued provider-artifact deduplication planning, keeping the CAND-014
  extraction boundary strictly at
  `src/atlas_agent/research/provider_mock_response_final_safety_seal.py`; no
  broadening is authorized.

## Post-release hardening landed in this release

- Added a shared `release_identity` pytest fixture in `tests/conftest.py` that
  loads current release identity from `docs/releases/release-metadata.json`.
- Refactored current-state tests to use fixture values instead of hardcoded
  release-identity literals, reducing cutover drift.

## Safety boundaries

This release enables nothing at runtime. Specifically, no candidate or document
in `v0.6.26`:

- enables live trading or makes live trading a default;
- enables live order submission or changes `can_submit`;
- enables broker or provider execution, or calls real brokers or providers;
- loads broker/provider credentials;
- adds network access;
- places, cancels, or flattens orders, or creates pending orders;
- mutates approval queues, the kill switch, heartbeat, deadman, or the audit
  hash-chain;
- broadens the CAND-014 provider-artifact extraction boundary.

`atlas run --mode live` remains fail-closed. This is a GitHub-only project and
PyPI remains unpublished.

## Known warnings (non-blocking)

1. Historical candidate-chain docs for pre-`v0.6.26` releases still emit expected
   warnings from `check_candidate_chain.py` because they reflect historical
   planning state rather than current metadata. Non-blocking.

## Not financial advice

Atlas Agent is a software tool, not a financial advisor. Trading involves
significant risk of loss. This plan makes no profitability, trading-safety,
live-readiness, production-readiness, or unattended-safe-operation claim.
