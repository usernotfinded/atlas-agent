# v0.6.14 Candidate Selection Gate

> **Status:** historical pre-release candidate-selection record
> **Not financial advice.** Atlas Agent is a software tool, not a financial
> advisor. Trading involves significant risk of loss.

This document preserves the candidate-selection gate used while `v0.6.14` was
planning-only. After separate owner authorization, `v0.6.14` became the current
public GitHub-only release. PyPI was not published. The statements below record
the pre-cutover candidate posture and do not expand release or runtime scope.

## Current State

- **Source/package version on `main`:** `0.6.14`
- **Current public release:** `v0.6.14` (GitHub-only)
- **Previous public release:** `v0.6.13` (historical)
- **Next planned release:** `v0.6.15` (planning only)
- **Canonical v0.6.13 public evidence:**
  [v0.6.13 Post-Release Evidence](v0.6.13-post-release-evidence.md) with the
  machine-readable companion `v0.6.13-post-release-evidence.json`.

## What CAND-002 Covers

CAND-002 is **paper portfolio stress constraints and drawdown guard**:

- Add `atlas backtest portfolio-stress` for deterministic, offline synthetic
  stress checks on paper portfolio proposals.
- Add [Paper Portfolio Stress Constraints](../paper-portfolio-stress.md),
  `scripts/demo_paper_portfolio_stress.sh`,
  `scripts/check_paper_portfolio_stress.py`, and
  `tests/test_paper_portfolio_stress.py`.
- Produce `paper-portfolio-stress.json` and `paper-portfolio-stress.md` with
  paper-only decisions: `paper_stress_pass`, `paper_stress_watchlist`,
  `needs_more_testing`, and `rejected`.
- Validate max stressed drawdown, max single-scenario loss, strategy
  concentration, minimum cash weight, weight sums, and excluded strategies.
- Integrate the checker and tests into local, CI, and quick release gates.

CAND-002 does **not** enable live trading, live submit, provider execution,
broker execution, or live promotion. It does not bump the package/source
version, create a tag, create a GitHub Release, or publish to PyPI.

## What CAND-007 Covers

CAND-007 is **paper portfolio evidence bundle and candidate closure gate**:

- Add [v0.6.14 Paper Portfolio Evidence](v0.6.14-paper-portfolio-evidence.md)
  and the machine-readable `v0.6.14-paper-portfolio-evidence.json`.
- Summarize implemented CAND-001 through CAND-006 paper portfolio work.
- Link each portfolio candidate to its primary docs, scripts, checkers, tests,
  and demos.
- Validate release identity remains `v0.6.13` public / `v0.6.14` planning.
- Add `scripts/check_v0614_paper_portfolio_evidence.py` and
  `tests/test_v0614_paper_portfolio_evidence.py`.
- Integrate the evidence checker and tests into local and CI gates.

CAND-007 does **not** enable live trading, live submit, provider execution,
broker execution, notifications, orders, or live promotion. It does not bump the
package/source version, create a tag, create a GitHub Release, or publish to
PyPI.

## What CAND-008 Covers

CAND-008 is **final paper portfolio release-readiness audit and Go/No-Go
dossier**:

- Add [v0.6.14 Final Paper Portfolio Release-Readiness Audit](v0.6.14-final-readiness-audit.md)
  and the machine-readable `v0.6.14-final-readiness-audit.json`.
- Validate CAND-001 through CAND-007 completion, paper-only posture, and gate
  integration.
- Validate source/package version remains `0.6.13`, current public release
  remains `v0.6.13`, and `v0.6.14` remains planning-only.
- Validate PyPI remains unpublished and no `v0.6.14` tag or GitHub Release is
  claimed.
- Add `scripts/check_v0614_final_readiness_audit.py` and
  `tests/test_v0614_final_readiness_audit.py`.
- Integrate the final readiness checker and tests into local, CI, and quick
  release gates.

CAND-008 does **not** authorize a release cutover, create a tag, create a GitHub
Release, publish to PyPI, enable live trading, enable live submit, call
providers, call brokers, send notifications, generate orders, submit orders, or
recommend any real allocation.

## What CAND-021 Covers

CAND-021 is **post-release hygiene consolidation** only:

- Make `v0.6.13-post-release-evidence` the canonical public evidence record.
- Archive or redirect stale v0.6.13 release-prep/cutover planning docs where safe.
- Preserve all auditability and historical evidence.
- Tighten the v0.6.14 candidate-selection gate.
- Add deterministic checker/test coverage for post-release hygiene.
- Update docs and gates minimally.

CAND-021 does **not** create a `v0.6.14` tag, GitHub Release, or PyPI
publication, and it does not bump the package/source version.

## What CAND-023 Covers

CAND-023 is **autonomous paper workflow demo and evidence gate**:

- Add `docs/autonomous-paper-workflow.md` describing an L1 paper-only autonomy demo.
- Add `scripts/demo_autonomous_paper_workflow.sh` to run the demo autonomously in a
  temporary workspace using only offline, no-credential CLI commands.
- Add `scripts/check_autonomous_paper_workflow_demo.py` to statically verify the
  demo, documentation, and release-metadata safety invariants.
- Add `tests/test_autonomous_paper_workflow_demo.py` covering checker pass/fail
  behavior and safety invariants.
- Link the new doc from README.md, governance, roadmap, public-launch-readiness,
  trust center, and reviewer checklist.
- Integrate the checker and tests into `scripts/dev_check.sh`, `scripts/ci_check.sh`,
  and `.github/workflows/ci.yml`.

CAND-023 does **not** enable live trading, live submit, provider execution, or broker
execution. It does not bump the package/source version, create a tag, create a GitHub
Release, or publish to PyPI.

## What CAND-024 Covers

CAND-024 is **paper mode offline provider isolation and no-network gate**:

- Wire the existing `NullProvider` into the provider factory so `model.provider = "null"`
  resolves to a deterministic offline provider.
- Make paper-mode agentic workflows fall back to `NullProvider` when the configured
  provider is missing credentials, so `atlas run --mode paper` works without an API key
  or network.
- Add the explicit `--offline` flag to `atlas run` and `atlas agent run` for a
  guaranteed provider-free paper path.
- Add `docs/paper-provider-isolation.md`, `scripts/check_paper_provider_isolation.py`,
  and `tests/test_paper_provider_isolation.py` covering the new gate.
- Update `scripts/demo_autonomous_paper_workflow.sh` to exercise the offline paper path.
- Link the new doc from README.md, governance, roadmap, public-launch-readiness,
  trust center, reviewer checklist, and candidate planning files.
- Integrate the checker and tests into `scripts/dev_check.sh`, `scripts/ci_check.sh`,
  and `.github/workflows/ci.yml`.

CAND-024 does **not** enable live trading, live submit, provider execution, or broker
execution. Live mode remains fail-closed. It does not bump the package/source version,
create a tag, create a GitHub Release, or publish to PyPI.

## What CAND-025 Covers

CAND-025 is **autonomous paper strategy evaluation matrix and promotion gate**:

- Add `atlas backtest compare` for deterministic, offline comparison of registered
  backtest strategies on bundled/sample OHLCV data.
- Add `docs/paper-strategy-evaluation.md`, `scripts/demo_paper_strategy_evaluation.sh`,
  `scripts/check_paper_strategy_evaluation.py`, and
  `tests/test_paper_strategy_evaluation.py`.
- Produce `strategy-evaluation.json` and `strategy-evaluation.md` with paper-only
  decisions: `paper_candidate`, `needs_more_testing`, and `rejected`.
- Rank strategies by conservative paper metrics and deterministic tie-breakers.
- Link the new doc from README.md, governance, roadmap, public-launch-readiness,
  trust center, reviewer checklist, and candidate planning files.
- Integrate the checker and tests into `scripts/dev_check.sh`, `scripts/ci_check.sh`,
  `scripts/release_check.sh --quick`, and `.github/workflows/ci.yml`.

CAND-025 does **not** enable live trading, live submit, provider execution, broker
execution, or live promotion. It does not bump the package/source version, create a
tag, create a GitHub Release, or publish to PyPI.

## What CAND-027 Covers

CAND-027 is **multi-regime paper strategy robustness reporting**:

- Add `atlas backtest robustness` for deterministic, offline evaluation across
  bundled synthetic OHLCV regime fixtures.
- Add `data/sample/regimes/ohlcv_uptrend.csv`,
  `data/sample/regimes/ohlcv_downtrend.csv`, `data/sample/regimes/ohlcv_flat.csv`,
  and `data/sample/regimes/ohlcv_volatile.csv`.
- Add `docs/paper-strategy-robustness.md`,
  `scripts/demo_paper_strategy_robustness.sh`,
  `scripts/check_paper_strategy_robustness.py`, and
  `tests/test_paper_strategy_robustness.py`.
- Produce `strategy-robustness.json` and `strategy-robustness.md` with
  paper-only decisions: `robust_paper_follow_up`,
  `regime_sensitive_needs_more_testing`, `needs_more_testing`, and `rejected`.
- Link the new doc from README.md, governance, roadmap, public-launch-readiness,
  trust center, reviewer checklist, and candidate planning files.

CAND-027 does **not** enable live trading, live submit, provider execution, broker
execution, or live promotion. It does not bump the package/source version, create a
tag, create a GitHub Release, or publish to PyPI.

## What CAND-030 Covers

CAND-030 is **paper autonomy evidence bundle and candidate closure gate**:

- Add [v0.6.14 Paper Autonomy Evidence](v0.6.14-paper-autonomy-evidence.md)
  and the machine-readable `v0.6.14-paper-autonomy-evidence.json`.
- Summarize implemented CAND-021 through CAND-029 paper-autonomy planning work.
- Link each candidate to its primary docs, scripts, checkers, and tests.
- Validate release identity remains `v0.6.13` public / `v0.6.14` planning.
- Add `scripts/check_v0614_paper_autonomy_evidence.py` and
  `tests/test_v0614_paper_autonomy_evidence.py`.
- Integrate the evidence checker and tests into local, CI, and quick release gates.

CAND-030 does **not** enable live trading, live submit, provider execution,
broker execution, or live promotion. It does not bump the package/source version,
create a tag, create a GitHub Release, or publish to PyPI.

## Candidate Selection Rules

A candidate is eligible for the `v0.6.14` planning line only if **all** of the
following are true:

1. **No live trading enablement.** It must not make live trading the default,
   remove fail-closed configuration requirements, or weaken live-mode gates.
2. **No broker/provider execution enablement.** It must not enable broker order
   submission, real provider/LLM execution, or direct AI-to-broker paths.
3. **No secrets or credentials.** It must not add real broker credentials,
   provider API keys, account IDs, portfolio values, or private financial data.
4. **No unsafe marketing claims.** It must not claim that returns are
   guaranteed, that trading is without risk, that live trading is safe, that
   production trading readiness exists, or that autonomous trading readiness
   exists.
5. **Protected runtime boundaries preserved.** Changes under
   `src/atlas_agent/config`, `src/atlas_agent/brokers`,
   `src/atlas_agent/execution`, `src/atlas_agent/safety`, or
   `src/atlas_agent/risk` are avoided unless explicitly justified, narrowly
   scoped, fully tested, and separately approved.
6. **Small candidate batches.** Candidates should be small, reviewable, and
   independently valuable.
7. **Checker/test/doc coverage.** Every candidate must have deterministic
   checker and/or test coverage, and docs updates where the change is visible to
   reviewers.
8. **Green CI before completion.** The candidate must pass `./scripts/dev_check.sh`,
   `./scripts/ci_check.sh`, and `./scripts/release_check.sh --quick` before it is
   considered complete.
9. **No release cutover side effects.** A candidate must not create tags,
   GitHub Releases, or PyPI publications, and must not bump the package/source
   version beyond `0.6.13`.

## Candidate Buckets

The following areas may be considered for `v0.6.14` candidate selection. They are
intentionally high-level and non-committal:

| Bucket | Examples | Safety note |
|---|---|---|
| Docs hygiene | Consolidate release-prep/cutover docs, fix stale links, improve cross-references. | Docs-only; no runtime or boundary changes. |
| Demo UX hardening | Improve offline demo scripts, paper-trading walkthrough reliability, sample-data smoke tests. | Local/offline only; no real broker/provider access. |
| Paper strategy evaluation | Compare built-in strategies on bundled sample data and produce paper-only candidate/reject/needs-more-testing reports. | Paper/offline only; no provider, broker, live, or release side effects. |
| Paper strategy robustness | Evaluate built-in strategies across deterministic synthetic regimes for paper-only follow-up. | Paper/offline only; no provider, broker, live, or release side effects. |
| Reviewer workflow simplification | Make public-launch-readiness and reviewer-checklist artifacts easier to consume. | Docs/checker only; no execution changes. |
| Broker/provider safety documentation | Clarify fail-closed defaults and credential-presence checks without enabling execution. | Documentation only; no adapter or execution changes. |
| Test runtime trimming | Isolate slow subprocess-heavy tests, improve parallel safety, preserve coverage. | Test infrastructure only; no runtime behavior changes. |
| Release tooling simplification | Refactor release checkers for reuse, reduce duplication between historical and current gates. | Checker/test only; no tag/release/PyPI behavior. |

## Explicitly Out of Scope

The following are **not** part of CAND-021 and require separate owner approval
when appropriate:

- Bumping the package/source version to `0.6.14`.
- Creating an annotated `v0.6.14` tag.
- Creating or publishing a GitHub Release for `v0.6.14`.
- Publishing `v0.6.14` to PyPI.
- Enabling live trading, broker order submission, or real provider execution by
  default.
- Modifying protected runtime boundaries without explicit justification and
  tests.

## Verification

```bash
# Canonical v0.6.13 evidence
python3.11 scripts/check_v0613_post_release_evidence.py

# v0.6.14 post-release hygiene gate
python3.11 scripts/check_v0614_post_release_hygiene.py

# CAND-025 paper strategy evaluation gate
python3.11 scripts/check_paper_strategy_evaluation.py

# CAND-027 paper strategy robustness gate
python3.11 scripts/check_paper_strategy_robustness.py

# CAND-030 paper autonomy evidence gate
python3.11 scripts/check_v0614_paper_autonomy_evidence.py

# CAND-002 paper portfolio stress gate
python3.11 scripts/check_paper_portfolio_stress.py

# CAND-007 paper portfolio evidence gate
python3.11 scripts/check_v0614_paper_portfolio_evidence.py

# CAND-008 final paper portfolio readiness gate
python3.11 scripts/check_v0614_final_readiness_audit.py

# Local gates
./scripts/dev_check.sh
./scripts/ci_check.sh
./scripts/release_check.sh --quick
```

## Safety Boundaries

Unless explicitly approved in a future candidate, these boundaries remain
unchanged for `v0.6.14`:

- **Live trading** disabled by default.
- **Live submit** disabled by default.
- **Provider execution** disabled by default.
- **Broker order submission** blocked for non-paper brokers.
- **No credentials, secrets, or API keys** committed.
- **No claims** that live trading is safe, that risk is absent, that profit is
  guaranteed, or that production/autonomous trading readiness exists.

---

*This candidate-selection gate was created after the v0.6.13 public release
cutover as part of CAND-021. `v0.6.14` remains a planning line only. CAND-030
records the paper-autonomy evidence closure gate without release side effects.
CAND-008 records the final paper portfolio readiness audit without release side
effects.*
