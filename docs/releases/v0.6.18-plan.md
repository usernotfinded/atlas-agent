# v0.6.18 Release Plan

Status: planning-only. No `v0.6.18` tag, GitHub Release, or PyPI publication
exists.

`v0.6.17` is the current public GitHub release. `v0.6.18` is the next planned
release line. The source/package version remains `0.6.17`. No release cutover
has been authorized.

## Candidates under consideration

| id | title | status | safety notes |
|---|---|---|---|
| CAND-010 | Safety-State Persistence Regression Guard | accepted in planning | Static checker scans the four CAND-009 safety persistence modules for fixed `<target>.tmp` write regressions; allows `atomic_write.py` as the unique `mkstemp`-based helper. No live trading, live submit, broker/provider execution, credential loading, network access, or order placement. |

## Acceptance

CAND-010 is accepted into the `v0.6.18` candidate chain with acceptance verdict
`PASS` on 2026-07-01. This acceptance is documentation/governance acceptance
only and does not authorize `v0.6.18` release cutover, live trading, live
submit, order placement, broker/provider execution, credential loading, network
access, or approval queue mutation.
